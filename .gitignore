node_modules/
dist/
.cache/
out/
*.log
.DS_Store

# Context pack: verbatim rules dumps, raw page HTML and the official card/diagram images
# are GW IP and are NOT redistributable. The whole pack stays local; tools/*/README.md
# documents the source URLs so any session can re-fetch it.
docs/context-pack/
docs/rules-source/
.cache/wahapedia/
__pycache__/
*.pyc
test-results/
playwright-report/

# Audit repros written by the rules-review workflow; promoted into the real suites or deleted.
tests/_audit/
