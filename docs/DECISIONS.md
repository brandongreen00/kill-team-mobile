# Decisions

Every judgement call, with the quoted rule text where one exists. Precedence when sources
disagree: Wahapedia (reflects the online update log, June 2026) > printed text.

| id | Decision | Rationale / source | Owner confirmation? |
| --- | --- | --- | --- |
| D-001 | **Stack: TypeScript + Vite + Preact, SVG board.** | Owner decision on TS+Vite. Preact over React: same API, ~4kB runtime, matters on a phone. SVG over canvas: crisp at any zoom, free hit-testing via DOM events, and terrain is a few hundred polygons — well within SVG's comfort zone. A DPR-aware canvas remains an option if profiling on a low-end phone shows a problem. | no |
| D-002 | **Close Quarters rules apply to Gallowdark AND Tomb World.** | The Killzones page says "Close Quarters — *Killzone: Gallowdark* has the following additional rules"; Tomb World's own section only says its rules are kept consistent with Gallowdark, and the Tomb World box rules include them. Owner decided both. Gated by `map.closeQuarters`, never by killzone id. | decided by owner — do not change |
| D-003 | **Guard is NOT universal.** | Guard is a Close Quarters universal action; core rules only reference it in Counteract ("excluding Guard"). The previous app offered it on every map — that was wrong. If the June 2026 update log makes it universal, flip `closeQuarters` gating, not the action. | flag if the update log says otherwise |
| D-004 | **The 6 Non-specific killzone maps are out of scope.** | Owner decision; they contain no terrain. | no |
| D-005 | **Model heights by base class** (`MODEL_HEIGHT_BY_BASE` in `src/core/state.ts`): 25mm 1.3", 28mm 1.35", 32mm 1.6", 40mm 1.9", 50mm/60×35 2.2", 75×42 2.5". | Visibility is drawn from the miniature's **head**, so a height is required. These are estimates from standard GW model proportions and are data, not hard-coded rules — a datacard may override via `Datacard.height`. | **yes — please sanity-check against your models** |
| D-006 | **Visibility sampling**: head point → 4 rings × 10 perimeter samples + top centre on the target's cylinder, target radius = 0.8× base radius, lowest ring lifted 0.12" so a line grazing the floor does not count. Bases never block ("Ignore operatives' bases when determining this"). | Core rules › Visible. A sampled silhouette is the standard approximation for a 1mm line to "any part of the miniature". | no |
| D-007 | **Targeting lines**: 12 perimeter samples on the shooter's base × the facing half of the target's base; 2D when heights match (<0.05"), 3D otherwise. | Core rules › Intervening: "from any point of its base to every facing part of the intended target's base... if there's a difference in height... three-dimensional". | no |
| D-008 | **Wall corners/ends** are modelled as 0.35"-radius zones at polygon vertices whose adjacent edges are ≥0.6" and turn by more than ~32°. | "only the corners and ends of Wall terrain can intervene... minor parts of the wall that protrude do not make a corner or end alone; it must be the main structure of the wall that turns a corner or ends." | no |
| D-009 | **"The active operative has passed it"** is modelled as the corner lying behind the shooter along the shooter→target axis. | Killzones › Wall terrain, with the printed diagram ("if operative A was wholly on the left side of the dotted black line…"). | no |
| D-010 | **Undo is limited to uncommitted input** plus the rules' own cancel-and-revert ("If an action is declared or begun but it's not possible to complete, the action is cancelled. Revert back to the game state before that action"). Once dice are rolled there is no undo. An optional house-rule undo may be added, off by default and logged in the replay. | The previous app's deep-snapshot undo is not the rules. | no |
| D-011 | **Devastating x is applied on retained crits before blocking**, and the success is still resolved afterwards. | "Each retained critical success immediately inflicts x damage... Note that success isn't discarded after doing so — it can still be resolved later in the sequence." | no |
| D-012 | **Default cover-vs-obscured choice for auto-resolution is obscured.** | Obscured costs the attacker a success *and* removes every crit; a cover save is one die. Applies only to auto/AI resolution — a human is always asked. | no |
| D-013 | **Objective markers are always on the killzone floor, except in Killzone: Bheta-Decima.** The extractor forces `z = 0` for every objective outside Bheta-Decima, whatever terrain footprint the marker is printed inside; `onFeatureId` is still recorded. Bheta-Decima keeps its printed heights (the condenser roof in maps 1-3, gantries elsewhere). | Appendix › GAME SEQUENCE: "Other than in Killzone: Bheta-Decima, all objective markers must be set up on the killzone floor." Owner confirmed 2026-08-17 ("ALL OBJECTIVES MUST BE ON THE KILLZONE FLOOR"), keeping the Bheta exception the brief documents. Three Volkus markers are printed inside a structure's footprint (volkus-4 P1 in Stronghold A, volkus-5 P2 and volkus-6 centre in large ruin C) — those are on the ground floor **under** the Ceiling/Vantage upper level, which is exactly what Ceiling terrain means. Pinned by `tests/integration.test.ts`. | confirmed by owner |
| D-014 | **A dotted/dashed outline on a map card means the piece goes slightly UNDER the adjacent raised terrain; a solid outline means it sits on top.** Applies to terrain pieces. The dashed rubble pieces inside Volkus strongholds and large ruins (G, I, J, K) are therefore on the killzone floor beneath the upper level, at `z0 = 0`. | Owner ruling 2026-08-17. This **overrides the brief's inference**, which read a white dashed rectangular outline inside a stronghold or large ruin as "a rubble piece placed on that structure's upper level" and asked for confirmation. The extracted data already had these pieces at `z0 = 0`, so no regeneration was needed. | confirmed by owner |
| D-015 | **"…can immediately perform a free 1AP action" is modelled as one extra AP restricted to the named action** (`grantFreeAction` in `src/teams/helpers.ts`), landing on that operative's next activation when the grant is made outside one. | The engine has no intent for performing an action outside an activation, and `canPerformAction` can only forbid, never permit. The bonus AP is always the last one the operative spends, so `canPerformAction` refuses anything outside the rule's named actions (and any move action when the rule says "it cannot move during that action") once it is being spent. Consequence: an operative already at −1 APL gets nothing, because APL changes clamp to ±1. Affects Kasrkin SEIZE THE INITIATIVE / RELOCATE / COVER RETREAT and Angels of Death WRATH OF VENGEANCE. | no |
| D-016 | **A ploy that needs a position or a target takes it from the intent's `data`; with none supplied it uses a deterministic, logged default** (the centroid of the friendly operatives the rule names, or the lowest-id eligible operative). | `UseGambit`/`UsePloy` carry a `data` payload but no decision channel, and a killzone position is continuous so it cannot be enumerated as `DecisionOption`s. The UI always passes `data`; the default exists so a bot-vs-bot game never stalls, and it is written to the log. Affects Kasrkin CLEARANCE SWEEP, Imperial Navy Breachers ATTACK/DEFENCE ORDER, Plague Marines CLOUD OF FLIES / NURGLINGS / VIRULENT POISON. | no |
| D-017 | **Angels of Death CHAPTER TACTICS are chosen through the pure setter `setChapterTactics(state, player, primary, secondary)`, and nothing applies until they are.** | "When selecting your kill team, select a primary and secondary CHAPTER TACTIC" — the Select Operatives step has no decision channel in the engine, and picking one silently would be worse than picking none. The same applies to CHAPTER VETERAN (`setVeteranTactic`). | flag if a Select Operatives decision channel lands | 
| D-018 | **"Once per turning point … if you roll two or more fails, you can discard one of them to retain another as a normal success" is offered as a `mode: 'fails'` re-roll grant.** | The retention step (`retentionOptions` in `dice.ts`) only knows Severe/Rending/Punishing and `onAttackDiceRetained` is declared but never emitted, so there is no seam for a team-specific retention transform. A single-fail re-roll is the closest the dice engine's decision vocabulary gets: it costs the same die and is offered to the same player at the same moment, but a re-roll can fail where the printed rule cannot. Affects Kasrkin RELICS OF CADIA, Angels of Death PURITY SEALS / INDOMITUS, Imperial Navy Breachers CLOSE ASSAULT. | **yes — worth an `onAttackDiceRetained` emit so this can be exact** |
| D-019 | **A weapon's "+1 to both Dmg stats" is applied where the damage is inflicted (`onDamage`), not by rewriting the profile.** | `WeaponProfile` objects come from the datacard catalogue and are shared by every operative in every battle; mutating one would leak across games (architecture rule 7). Affects the rare `Toxic` rule and Imperial Navy Breachers CLOSE ASSAULT. | no |
| D-020 | **A team rule that the hook surface cannot express gets a small additive core seam rather than being left a no-op.** Batch 1's second half added: `onSelectTarget` (target substitution) and `onStunTest` (new hooks, both emitted); `onValidTarget.viewFrom` + `ignoreFriendlyControlRange` (payload fields); the previously-dead `onValidTarget.forceVisible` honoured; and `onSelectWeapon` actually emitted. | CLAUDE.md architecture rule 5: "an unknown hook name is a build error … never a silent no-op" — a *declared but unemitted* hook is exactly the silent no-op that rule forbids, and four printed rules (HOLY DEFENDER, SAVIOUR PROTOCOLS, Magnify, PSYK-OUT GRENADES) have no other honest expression. Every seam is listed in docs/TEAM-STATUS.md § "Engine seams added for team rules". | no |
| D-021 | **A rule that grants an action the universal one forbids is registered as its own `ActionDef` with `treatedAs` pointing at the universal action.** Kommandos `Charge (Throat Slittas)` (Conceal), `Shoot (Explosive)` (while engaged, self-targeting), `Fight (Krumpin' Time)`, `Smoke/Stun Grenade (Taktical Wot-notz)`. | `canPerformAction` can only forbid, never permit, and the universal action's own `check` is the blocker. `treatedAs` keeps action restrictions shared, so the carve-out can never be used twice. Follows the Kasrkin `Shoot (Rapid Fire)` precedent. Consequence: `src/ai/legal.ts` only builds movement params for the four universal move ids, so the AI never uses `Charge (Throat Slittas)`; a human can. | no |
| D-022 | **"You can use this rule" is auto-used on a stated, deterministic policy when it is free, and raised as a `PendingDecision` when it costs something.** AUTO-FLAGELLATOR (self-damage) asks; SAINTLY RELICS fires only against damage of 3+ or a killing blow; HOLY DEFENDER redirects only when the ABJUROR has more wounds left than the operative it shields; Boom! always rolls two D6 because the second is free. | The engine cannot ask a question inside `onDamage` without stalling a dice sequence, and a decision on every attack dice would drown the log. Each policy is written next to the rule it governs and logged when it fires. | no |
| D-023 | **Reanimation Protocols' Ready-step roll is a real decision** (which marker, and the order the operative comes back with), and an operative REANIMATED during that step still gets its Living Metal wounds. | "select one of your Reanimation markers and roll one D6" is a player choice, and Living Metal resolves "after resolving all other rules in this step (e.g. Reanimation Protocols)". A REANIMATED operative keeps its operative id, so the Martyrs tac op's per-victim ledger cannot score it twice — pinned by a test in `tests/teams/hierotek-circle.test.ts`. | no |
| D-024 | **Kommandos' Boom! resolves as direct Blast damage instead of a free Shoot action.** The 2D6 is rolled as printed; on a 4+ each operative within the explosives' Blast radius takes its Normal Dmg, with no defence roll. | "this operative performs a free Shoot action with its explosives before it's removed from the killzone" happens inside `inflictDamage`, i.e. inside another in-flight shoot/fight sequence — starting a second sequence there would clobber the first (`state.sequence` is single-slot), and `onIncapacitated.freeActions` is declared but never consumed. Flagged in docs/TEAM-STATUS.md; the honest fix is a queued free-action seam. | **yes — worth a queued "free action" seam so death triggers can shoot properly** |
| D-025 | **Anti-PSYKER is implemented as the Celestian Insidiants' own printed footnote: "it has the Lethal 5+ weapon rule".** `data/teams/_rare-weapon-rules.json` defines the shared rule as "add 1 to both Dmg stats … and it has the Lethal 5+ weapon rule", sourced from the Novitiates' datacard. | Precedence is the team's own printed text for its own weapons (the rule is footnoted on each Celestian datacard). The discrepancy is real and belongs to whoever implements the Novitiates: either the two teams print different versions, or the shared definition is wrong for one of them. | **RESOLVED by D-033** — checked against both cards while implementing the Novitiates: the two teams genuinely print different rules, so batch 1's Celestian implementation was right and the shared registry was wrong for them. No owner action needed. |
| D-026 | **A unique action's whole legality — target, range, visibility, once-per-turning-point — lives in `check`; `perform` may only fail for reasons `check` already excluded.** | `src/ai/legal.ts` `missionCandidates` tries plausible params (including friendly targets) and offers anything whose `check` passes; a `perform` that then refuses becomes a REJECTED INTENT, which the soak bar forbids. Found the hard way: the first Celestian soak rejected SPEAK OF HER DEEDS, and the same shape is still live in Plague Marines' POISONOUS MIASMA. | no |
| D-027 | **Bheta-Decima gantry deck height = 3.0"**, and the condenser roof with it. | No published measurement exists for the Bheta-Decima gantries. 3.0" matches the Volkus stronghold first floor, sits inside the 3" climb reach from the killzone floor and above the 2" free-drop threshold. **Owner confirmed 2026-08-17** ("3 inches is a fine assumption"); `data/terrain/bheta-decima.json` now records `owner-confirmed` rather than `assumed`. | confirmed by owner |
| D-028 | **`onCounteract` is emitted before the Engage-order test, which becomes the event's default rather than a pre-filter.** `counteractCandidates` now emits for every expended, not-yet-counteracted operative with `allowed: o.order === 'engage'`, so a team rule may widen it; `whoActivates` gained an optional `ctx` and consults `counteractCandidates` when given one. | "This operative can counteract regardless of its order" is printed on the Astartes faction rule of **twelve** kill teams (ten implemented). Filtering before the emit meant the hook could only ever narrow the list the core had already computed, so every one of those clauses was unreachable — and batch 1 shipped Angels of Death and Plague Marines with the handler registered but inert, which is precisely the silent no-op CLAUDE.md rule 5 forbids. `whoActivates` had to move with it or the candidate list and the turn test would disagree; as a side effect the two now also agree about the On Guard lockout, which `whoActivates` did not check. Costs one line of `src/ai/legal.ts` (passing the ctx it already holds). | no |
| D-029 | **A printed `maxCount` whose role matches no selection-list row falls back to a datacard KEYWORD match** (`src/teams/selection.ts`). | The Deathwatch print "your kill team can only include up to one GRAVIS operative" scrapes as `{kind:'maxCount', role:'GRAVIS'}`, but GRAVIS is a keyword shared by the BOMBARD, BREACHER and HORDE-SLAYER rows rather than a selection role. Matching `entry.role` only made the constraint dead, and `defaultRoster` produced a two-GRAVIS kill team that `validateRosterFor` then accepted. Extending the vocabulary in the shared validator is the fix CLAUDE.md asks for; a per-team `if` is not. | no |
| D-030 | ~~**`maxItem` selection constraints stay unenforced for now**~~ — **SUPERSEDED by D-036.**
| D-031 | **`onRollAttack` is still NOT emitted by `fight.ts`, deliberately.** Six of the eight batch-2 teams need it and each says so in its notes column. | Every melee re-roll rule in the game is unreachable without it, so it is the highest-value seam left — but 15 of the 17 existing `onRollAttack` handlers across batches 1 and 2 do not guard on `ev.ctx.type`. Emitting it in fights would silently change melee behaviour for all of them: some print "shooting, fighting or retaliating" and would become more correct, others are shooting-only and would start firing where they should not. It needs its own change with all 17 handlers audited, not a blind add inside a teams PR. | **yes — worth scheduling; it unblocks Designate Target, Cast the Runes, CHAOS TALISMANS, Screecher, RAVAGE DESTINY, FATE ITSELF IS MY WEAPON and PURITY SEALS** |
| D-032 | **`onSelectWeapon` is emitted from the Shoot action's `check` as well as from `startShoot`, and its payload carries `dryRun`.** A handler that changes state must return early on a dry run. | The rare `Concealed Position` rule is a PROFILE-level restriction (`availableWeapons` is per weapon), so `onSelectWeapon` is the only hook that can express it — but it fired only inside `perform`, so `src/ai/legal.ts` offered a shot the sequence then refused, and a `perform` failure `check` could not predict is a rejected intent (D-026). The batch-3 soak caught it as spectre-squad rejecting its own Long-las. The dry run sees exactly what the real emit sees: cover and obscured are unknown at that point in the sequence and were already passed as `false`. `dryRun` rather than a purity requirement because 6 of the 10 handlers across three batches use the hook for bookkeeping — Ratlings uses it as a start-of-sequence seam. | no |
| D-033 | **A rare weapon rule's definition is resolved against the kill team that uses it** (`rareRuleTextFor`), not taken globally from `data/teams/_rare-weapon-rules.json`. Resolution order mirrors the normaliser's: a datacard ability of that name → a faction rule of that name → a `*Name:` footnote inside a faction rule → the shared registry. | **This answers D-025.** The two teams genuinely print different Anti-PSYKER rules: the Novitiate Condemnor's datacard ability says "add 1 to both Dmg stats of this weapon **and** it has the Lethal 5+ weapon rule", while the Celestian Insidiants' footnote inside "Weapons of the Witch Hunters" says only "it has the Lethal 5+ weapon rule". Batch 1's Celestian implementation was therefore correct and the registry was showing those players the wrong text. Detonate is the same shape across four teams ("your Mine marker" vs "your Explosives marker"). Pinned by `tests/rare-weapon-rules.test.ts`. | **resolved — no owner action needed** |
| D-034 | **A `kind: 'every'` selection group is a FIXED roster: each printed row must appear exactly its own `count` times.** | "Every ELUCIDIAN STARSTRIDER operative in the following list: 1 ELUCIA VHANE / 1 CANID / … / 3 VOIDSMAN / 1 VOIDSMAN". The validator checked only the group TOTAL and ignored each entry's `count`, so `defaultRoster` fielded ELUCIA VHANE plus nine CANIDs and `validateRosterFor` called it legal — and the soak had been playing it. Affects elucidian-starstrider and gellerpox-infected. | no |
| D-035 | **A `sameAsAbove` selection group is folded into the list group it refers back to**, and a role carrying an explicit printed `maxCount` is exempt from the blanket "each operative on this list once". | "5 INQUISITORIAL AGENT operatives selected from the list above, or REQUISITIONED operatives…" is a group no selection entry belongs to, so nothing could fill it; and "…you can include up to two GUN SERVITOR operatives" scrapes as a `maxCount` next to the uniqueness constraint, which contradicted it. Together these made the largest kill team in the game unfieldable. The REQUISITIONED branch itself is still unreachable — its seven datacards have no selection rows and the faction rule draws from six OTHER kill teams' lists. | no |
| D-036 | **`defaultRoster` tries each row's OTHER options before giving up, which let `maxItem` and `exclusiveItems` finally be enforced.** Supersedes D-030. | The check itself was always two lines over weapons the validator already resolves; what blocked it was `defaultRoster` always taking `loadouts[0]`, so a capped weapon sitting first on several rows made the team unfillable (void-dancer-troupe could field 2 of its 7). The fill loop now probes a row's option variants and takes the first that validates. Enforcing both turned up a SECOND bug immediately — Hunter Clade could not field a legal kill team either (D-037) — which is exactly why the constraints were worth turning on. Still unenforced: `kind: 'custom'` (warpcoven, inquisitorial-agent, brood-brother, blooded, gellerpox-infected), which by definition needs a per-team hook. | no |
| D-037 | **`uniqueExcept` matches a role KEYWORD against the printed row's space-separated tokens, not by exact equality.** | "Other than GUNNER and WARRIOR operatives, your kill team can only include each operative on this list once" (Hunter Clade), where the scraped rows keep their sub-group prefix — `SKITARII RANGER GUNNER`, `RANGER WARRIOR`, `WARRIOR SICARIAN`. Exact equality never matched, so the printed exemption never fired and the team could not field a legal kill team. Matching is on space-separated tokens **deliberately, not substrings**: the Kasrkin exemption for `TROOPER` must not also exempt `RECON-TROOPER`, `VOX-TROOPER` or `DEMO-TROOPER`, which are different operatives that merely end in the same word. Both halves are pinned by tests. | no |
| D-038 | **The `uniqueExcept` keyword fallback (D-037) applies only when the list prints no row of that exact name.** | Two teams need opposite answers from the same printed sentence. Hunter Clade exempts "GUNNER and WARRIOR operatives" and prints only prefixed rows (`SKITARII RANGER GUNNER`, `RANGER WARRIOR`), so the exemption must reach them. Hearthkyn Salvager exempts the same two words and prints a plain `WARRIOR` row **beside** `JUMP PACK WARRIOR`, so there it names the plain operative and the jump-pack one stays unique — D-037's unconditional token match made the default roster field three of them. Kasrkin is the same shape a third time (`TROOPER` beside RECON-/VOX-/DEMO-TROOPER). Caught by the hearthkyn-salvager agent reviewing its own roster, and pinned in both directions. | no |
| D-039 | **A dashed rectangle (D-014) is recognised by geometry, not by counting dash segments — and a fitted-template IoU below 0.85 fails the build instead of warning.** `dashed_rects()` now tests that the dashes lie on a rectangle's perimeter and cover enough of the *visible* perimeter, discounting the stretch an opaque label pill or objective marker hides; `validate_maps.py` gained a hard `IOU_FAIL = 0.85` tier with a named, reasoned `IOU_ALLOW` list that is itself checked for staleness. | The old `parts >= 5` gate was scale-dependent: piece I (2 × 3.5") carries ~20 dash segments, piece K (0.75 × 1.979") ~8, and the label pill — printed **on** the outline on these cards, not inside it — hides two to four more. Every small dashed rectangle on every Volkus card was therefore missed, and a miss is not a null result: the piece stays in `chips`, `split_blob_by_chips()` hands it a slice of the neighbouring green blob, and **both** pieces ship wrong geometry (volkus-3 K + D, volkus-5 J + C, volkus-6 K + A). `fit_templates()` measured the damage and recorded it as `qa.templateIoU`, but G7 only warned, so it shipped anyway — a gate that only warns is not a gate. Pinned by `tools/maps/test_extract.py` (synthetic cards; the real cards are GW IP and not in the repo) and by `tests/maps-rubble.test.ts` (every rubble piece is a 4-vertex rectangle at its template size). | **yes — the six affected features cannot be fixed without re-running `pnpm maps:extract` against the local context pack; they stay allow-listed until then** |
| D-040 | **A `maxItem` cap matches its weapon with a tolerance for the printed plural.** | The XV26 Stealth Battlesuits print "Your kill team can only include up to two fusion **blasters**" while the datacard weapon is "Fusion **blaster**", so the exact match counted zero and the cap — newly enforced by D-036 — was dead on arrival. `sameItem` accepts `x`, `xs` and `x` vs `xs` in either direction. Found by the xv26 agent, which had written a test pinning the cap as dead; that test now pins it firing. | no |
| D-041 | **A `uniqueExcept` exemption also matches when the exempt NAME is longer than the printed row**, not only when it is shorter. | Two opposite shapes of the same sentence. Hunter Clade exempts "GUNNER" and prints `SKITARII RANGER GUNNER` (exempt word shorter — D-037). Wrecka Krew exempts "BREAKA BOY FIGHTER" and "TANKBUSTA GUNNER" while printing the rows as plain `FIGHTER` and `GUNNER` (exempt name longer). Both directions are gated by D-038's rule — the fallback fires only when the list prints no row of that exact name — so Kasrkin's `TROOPER` and Hearthkyn's `WARRIOR`, which do print such a row, are unaffected. | no |
| D-042 | **`defaultRoster` stays first-legal-row-wins in printed order; a coverage-maximising roster for the soak is a SEPARATE concern.** | Nine agents across batches 3–5 reported the shared soak never exercising rules because the default roster repeats the first repeatable row (xv26 fields 5 of 8 datacards, brood-brother 9 of 15, inquisitorial-agent 11 of 18). Making the fill prefer un-taken rows lifted mean datacard coverage to 83% — and broke 10 tests across 8 files, including two batch-1 rule tests that legitimately rely on the default pairing specific operatives, and **changed the character of a team**: Blades of Khaine's printed default is deliberately mono-Aspect, which is exactly what triggers its "twice per turning point" clause. `defaultRoster` is documented as *the printed default* and five batches of tests depend on that determinism, so the change was reverted. The soak should build its own coverage-maximising roster instead. | **worth doing — it is the largest remaining gap in what the soak actually exercises** |
| D-043 | **`RE_MAX_ROLE` in `tools/teams/constraints.py` matches the role CASE-SENSITIVELY and captures a trailing "with <weapon>" qualifier**, and a qualified cap is emitted as a `maxItem` scoped by `role` rather than as a role-level `maxCount`. | Two bugs in one regex, both of which made a printed constraint dead or contradictory. (1) The pattern carried a blanket `(?i)`, so its uppercase `ROLE` class matched lowercase prose too and "up to one SURVEYOR **operative and up to five** SICARIAN operatives" was captured as a single role — one dead constraint where two were printed (hunter-clade, wyrmblade). (2) "up to one COMBAT SERVITOR operative **with meltagun**… up to three **with incendine igniter**" dropped the weapon, leaving two contradictory role caps whose stricter row made the Battleclade's 8-operative group unfillable — the last team that could not be fielded. The committed `constraints` arrays for the three affected teams were regenerated from their own `selection.rawText` with `emit_custom=False`; **the other 45 teams reproduce byte-identically**, which is what validates that regeneration path. `validateRosterFor` now honours a `maxItem`'s optional `role` scope. | no |
| D-044 | **A `groupCap` (and `halfSelection`) matches footnote MEMBERSHIP, not string equality.** | A selection row can carry several footnote markers, and the scraper joins them: the Blooded plasma-gun GUNNER is `footnoteGroup: "^1,2"`, the only such row in all 48 teams. Matching `entry.footnoteGroup === '^1'` let that row escape its own printed cap — "^1 You cannot select more than three of these operatives combined" — so `defaultRoster` fielded FOUR of a maximum three and `validateRosterFor` accepted an illegal kill team. Found by the blooded agent and verified directly. `footnoteMarkers()` splits `"^1,2"` into `['^1','^2']`, restoring the `^` on the bare digit. | no |
| D-045 | **`LoadoutOption.choiceGroups` is modelled and resolved, and a weapon a choice group OFFERS is not "always available".** | "Autopistol **or** laspistol; chainsword **or** power weapon" is ONE printed option with two choice groups — pick one from each. `LoadoutOption` was `{id, label, weapons}` only, and the scraper leaves `weapons` empty for such an option **and** copies every alternative into `alwaysWeapons`, so `weaponsForPick` handed the operative all of them. Four teams were fielding the wrong weapons: Blooded CHIEFTAIN got both pistols, Hunter Clade's two GUNNER rows got all three of arc rifle / plasma caliver / transuranic arquebus, XV26's SHAS'VRE got both heavy weapons, Death Korps' CONFIDANT got all four of its two either/or pairs. **Batches 3, 4 and 5 each reported a symptom of this as a per-team data oddity; none found the cause**, which was a field the type never declared. A chosen option now contributes exactly one weapon per group — the one named in `pick.weapons`, else the first. | no |
| D-046 | **The phone has no tab bar. One board, always mounted, with a command sheet over it.** | Apple HIG: "Use a tab bar to support navigation, not to provide actions." Material: "Don't use a navigation bar for fewer than three destinations." During a battle this app has one destination — the killzone — and the other three tabs were actions or a feed. The measured cost of the old model was four navigation taps per operative placed: 24+ tab switches before the first die. `commandPlan(state)` derives the single current screen instead, so there is no navigation state that can disagree with the game state. | no |
| D-047 | **The board's viewport is aspect-locked to its PANE, not to the 30x22 killzone.** | Locked to the board, a portrait phone drew the killzone as a 390x286 strip with ~400px of dead black — 58% of the pane wasted and a 32mm base at 16px. Locked to the pane, the board fills what it is given and the long axis pans. `fitViewport` still exists for the ⤢ control, which is the only place letterboxing is a deliberate choice. | no |
| D-048 | **A phone held sideways gets a SIDE sheet, not a bottom one, below 560px of height.** | A bottom sheet needs ~200px of a 390px-tall screen. Side-docked, the same content leaves the board 405x281 instead of 405x88. Three columns start at 1200px, not 900px, for the same reason: at 900px a 360px rail plus a 320px log leaves the board 220px. | no |
| D-049 | **Alternating deployment in thirds and gambit alternation are core selectors (`deployToAct`, `gambitToAct`), not UI logic.** | Both are printed rules the reducer does not enforce (`DeployOperative` checks ownership, drop zone, hazardous terrain and base overlap and nothing about turn order). Derived in the UI they were rules that only the app knew and no test covered. They now live in `core/phases.ts` quoting their text, with tests. The reducer still does not ENFORCE them — that would change what the AI runner may dispatch — so a driver can still deploy out of order. | flag if you want the reducer to enforce it |
| D-050 | **`baseGap` clamps at zero, so `baseGap(...) < -1e-4` is a dead comparison — fixed in `DeployOperative` ONLY.** | Nine sites across `reducer.ts`, `movement.ts` and seven team modules use that expression to mean "these bases overlap"; none of them has ever fired, so two operatives could be set up or finish a move on the same square inch. `canDeployAt` now uses `basesOverlap`. The other eight are NOT changed: they govern legal move END POSITIONS, and switching them fails sixteen rules tests whose fixtures place models at overlapping centres. Deciding whether those tests or the guard are wrong is a rules question, not a UI one. | **yes — 8 dead overlap guards remain, please decide** |
| D-051 | **Undo is empirical, not declarative: an intent is undoable iff it moved neither the RNG cursor nor the roll journal.** | Keying undo on `Intent['t']` is wrong here — `Reposition` rolls a D3 when it walks onto a mine and `Breach` rolls a D6 per adjacent operative, so a "movement is safe to undo" list would hand the player a re-roll. This keeps D-010's rule ("once dice are rolled there is no undo") without having to know in advance which intents roll. | no |
| D-052 | **The end of a turning point is a screen the players stop on, so `advanceTurningPoint` no longer rolls into the next one in the same call.** `AdvancePhase` out of `endOfTP` is what increments `state.turningPoint`. | `advanceTurningPoint` set `state.phase = 'endOfTP'` and then overwrote it with `'strategy'` a few lines later, so the phase never existed for a moment anything could observe: `endOfTpPlan` was unreachable and the only scoring the game does — up to 6VP a side per turning point, resolved with nobody touching the screen — landed silently while the player was looking at the next initiative roll. `src/ai/runner.ts` already dispatched `AdvancePhase` for `endOfTP`, so bot games walk through the new stop unchanged; one wiring test asserted the collapsed behaviour and was updated to assert the stop. | no |
| D-053 | **Equipment set-up is a real setup step (`setup.step = 'placeEquipment'`), entered from `BeginDeployment` and left when `equipmentToAct` returns null.** | The step was in `SetupState['step']`, `PlaceEquipment` worked, and `validateEquipmentPlacement` was complete with per-item constraints — but nothing ever set the step, so equipment a player paid one of their four picks for was never placed. Alternation follows the printed rule and lives beside `pendingEquipmentItems` as `equipmentToAct`, mirroring D-049. `SkipEquipmentPlacement` now records `setup.equipmentDone[player]` so a player whose item has nowhere legal to go can leave the step instead of stranding the battle, and the reducer's duplicate `equipmentPlaced` increment (the core function already counted it) is gone. | no |
| D-054 | **The equipment screen shades where an item may go by sampling `validateEquipmentPlacement` across the board, not by highlighting the drop zone.** | Every option carries its own constraints — the Ammo Cache goes anywhere in your *territory*, a barricade must clear other terrain, a ladder needs something to lean on, and Bheta-Decima adds an exception of its own. A drop-zone spotlight was simply the wrong area, and the first build shipped it. Sampling the engine's own predicate means the shaded area and the answer you get on tap cannot disagree. Costs ~1,100 predicate calls per item, memoised on `state.seq`. | no |
| D-055 | **A one-finger ghost drag is for a FINGER, and only when a base is actually being aimed. A mouse pans on drag and previews the ghost on hover.** | Any arm carrying `commit` used to capture the single pointer, which (a) drew a phantom 32mm disc on the two screens that take a `commit` with no base — the drop-zone picker and the shooting screen's tap-swallower — and (b) left a desktop mouse, which has exactly one pointer, with no way to pan at all on the deploy and move screens. | no |
| D-056 | **"Allocate manually" is a screen, not a second button that ran the automatic allocation.** | The `allocateDefence` decision offered `auto` (carrying the allocation as option data) and `manual` (carrying nothing), and `resolveDecision` falls back to auto whenever the payload has no `unblockedCrits` — so the manual option silently ran auto on the one screen that decides how much damage an operative takes. The replacement states the choice the way the rules do: the incoming hits as chips, tap one to save against it, with the allocator spending the cheapest legal defence dice and refusing what cannot be paid for. | no |
| D-057 | **The pass-and-play handover is derived from who is acting (`deviceHolder`), and applies during the battle, not only during setup.** Sandbox mode skips it. | `ui.handedOverTo` was set for the secret loadout screens, cleared at the reveal, and never set again — so every reactive window belonging to the other player (defence allocation, the On Guard interrupt, the secret primary op) was shown to whoever happened to be holding the phone. The device is with whoever is activating; a reactive window is by definition asked of the other player, which is exactly when it has to change hands. Resolving or passing hands it back. | no |
| D-058 | **The zoom cluster's side is a property of the KILLZONE, decided once, not of the current screen's framing.** | It was derived from `plan.frame`, so it hopped corners between deploy and activate and back. A control that moves is a control you re-find every time — the "where did that go?" feeling this overhaul exists to remove. It still dodges the drop zones; those are a property of the map and do not move. | no |
| D-059 | **A `CommandPlan`'s own `detent` is authoritative; "a board-arming screen must not be covered by its own sheet" is only the DEFAULT for a plan that states none.** | They were two effects, and the second ran last, so it won every time. `firefight.activate` arms the board AND asks for `half` ("tap one of your ringed operatives, **or pick it from the list below**"): forced to `rest`, that list rendered ~75px below the bottom of the screen, so for four turning points the only way to activate anyone was to hit a 44px token. `firefight.shoot` had the same shape. Every plan now states the detent it needs, and `e2e/smoke.spec.ts` pins that the list a screen points at is on screen and clickable. | no |
| D-060 | **A screen's running answer and the button that commits it belong in the PEEK, not in the sheet's scrolling body.** Applied to defence allocation, whose in-progress allocation therefore lives in `UiState` keyed by decision id. | The peek is the only part of the sheet guaranteed to be on screen at every detent. Built inside the allocator component, the damage total and "Take N damage" were below the fold on a phone — on the screen that decides how much damage an operative takes. Keying the state to the decision id is what stops the previous attack's allocation leaking into the next one. | no |
| D-061 | **`ENFORCED_KINDS` in the roster builder is a copy of `validateRosterFor`'s switch and must match it exactly.** `maxItem` and `exclusiveItems` were enforced but missing from it. | The list decides which printed rules the builder discloses as "rules this app does not check". Having drifted, the screen told the player a rule was unpoliced while refusing their roster on it — Battleclade rendered "2 rules this app does not check" directly above a `+` button those two rules had disabled, once D-043 regenerated its caps from `maxCount` into role-scoped `maxItem`. The two now-dead description sentences were deleted rather than left as unreachable code that lies. Three kinds are still genuinely unenforced (`exclusive`, `custom`, `distinctOptions`) so the disclosure still earns its place, and a test now pins BOTH directions: a rule the validator checks is never listed, and one it cannot is. | no |
| D-062 | **An inline "A or B" alternative (`LoadoutOption.choiceGroups`) gets its own picker, chosen by weapon NAME into `pick.weapons`.** | D-045 correctly stopped `alwaysWeapons` from handing an operative every alternative at once, and `weaponsForPick` resolves the choice from `pick.weapons` with the first alternative as its default. But the builder only ever wrote `loadoutIds`, so the default was the only answer the app could give: it rendered the whole printed sentence — "Arc rifle, plasma caliver or transuranic arquebus" — as one un-splittable option. Both Hunter Clade gunners therefore took an arc rifle, and because the team prints a cap of one arc rifle, **a legal two-gunner roster could not be built in the app at all**. Six entries across five teams were affected (hunter-clade ×2, blooded, death-korps, xv26). A choice group is a different shape from an option group — bare weapon names, no ids — so it needs its own control rather than being folded into `choiceGroups()`. | no |
| D-063 | **"Always carried" is computed after subtracting what a choice group offers.** | The scraper leaves every alternative in `entry.alwaysWeapons`, and D-045 made `weaponsForPick` ignore them there; the card did not, so it tagged the one weapon the player was choosing between as fixed kit — "Arc rifle — always carried" sitting directly under the picker that changes it. Mirrors the `offered` set in `weaponsForPick` rather than reimplementing the rule. | no |
| D-064 | **A move increment is checked against terrain along its CENTRE LINE, not as a swept base.** The full base is still checked where the operative finishes (`baseBlockedByTerrain`), and where it starts. | Killzones › Terrain and Movement: "Operatives cannot move through terrain — they must move around, climb over or drop/jump off it." Until now `validateMove` checked only the END of a move, so a Reposition in a straight line through a stronghold wall was accepted — the bug the owner reported. The centre line is the same test `accessibleCrossings` and `obstructingCrossings` already use, and it is what Accessible terrain is defined against ("only the centre of an operative's base needs to move through"). A swept-base test would be stricter than the rules in one place that matters: `data/maps/**` is machine-extracted with known IoU noise, and a 32mm base is 1.26" wide against doorways and Gallowdark access points of 1.2-1.9", so sweeping the base would seal openings the cards draw as open. | **yes — flag if you want the stricter swept-base test** |
| D-065 | **An increment that changes level does not collide with the terrain feature it is climbing onto or dropping from**, only with everything else. | Same sentence: the rule permits "climb over or drop/jump off *it*". Volkus walls are extruded to the piece's maximum height (Stronghold A's ring is 5.906" tall, its floor 3.0"), so testing a climb against the wall it climbs would seal every building from the outside and every roof from the inside. Purely horizontal increments get no exemption, so an operative on a stronghold's upper level still cannot walk out through its wall — it has to drop. | no |
| D-066 | **The Volkus doors `_volkus_doors` could not read off the cards are derived from the hole they leave in the wall ring** (`tools/maps/doors.py`), and any wall bar traced across an opening is clipped back out of it. | Killzones § Stronghold B / Large Ruin C: "The door is Accessible and Heavy terrain." The card detector demands a white dashed blob 12-14px on its short side and in practice only ever fired on Stronghold B, so 18 of 24 doorways shipped as unmodelled holes: free to cross, no cover, obscuring nobody — and, once D-064 landed, too narrow for a 32mm base, which would have sealed the buildings. The derivation is validated, not assumed: told nothing but wall geometry it reproduces all six doors the extractor DID find, to the rounding of the stored polygons (`tools/maps/test_doors.py`). One instance (volkus-5 Stronghold A, whose top wall is traced as three sub-thickness slivers) falls back to the consensus position of the same piece across the maps that resolved. | **yes — the derived doors are worth eyeballing against the cards** |
| D-067 | **The shell registers `ALL_TEAM_MODULES`, not a batch.** | `src/ui/App.tsx` had passed `BATCH_1` since phase 5, so 40 of the 48 selectable kill teams played whole battles with no faction rules, ploys, equipment or unique actions — silently, since `rebuildHooks` optional-chains a missing module. Every module was already written, imported by `src/teams/index.ts` and covered by the soak; only the shell's hand-off was stale. Registering all 48 costs 30 bytes in the bundle, because the barrel already pulled them all in. | no |
| D-068 | **Cover has two answers, not one: `inCover` decides the cover SAVE, `inCoverForTargeting` decides the VALID TARGET test.** A rule that means "cannot be in cover" at all sets the new `onValidTarget.denyCover` instead. | Appendix › Seek and Killzones › Vantage both say the same thing in the same words: "Whilst this can allow such operatives to be targeted (assuming they're visible), it doesn't remove their cover save (if any)." One boolean could not express that, so Seek, Seek Light and the Vantage Light denial were each deleting a cover save the rules explicitly preserve. Two team rules had already worked around it: Exaction Squad's GUILT REVEALS ITSELF recomputed the cover at the Roll Defence Dice step to put the save back (now deleted — the core does it), and Exodite Dragon Masters' DRACONIC CAVALRY TACTICS used `ignoreCoverTerrain` for a rule that really does remove cover, and now uses `denyCover`. | no |
| D-069 | **Vantage's "ignore Heavy terrain connected to Vantage terrain" is applied to obscured only** (`CoverOpts.ignoreForObscured`), not as a global exclusion from intervening. | Killzones › Vantage: "**Thirdly, for the purposes of obscured**, ignore Heavy terrain connected to Vantage terrain the active operative or the intended target is on." Passed as `CoverOpts.ignore` it removed the part from `interveningParts` altogether, so a Heavy part within 1" of the target — which cannot obscure anyway, by the 1" rule — stopped giving cover too. | no |
| D-070 | **The terrain an operative is standing on is never intervening between it and anyone else.** | Core rules › Intervening: "Anything at least one of these lines cross is intervening." A targeting line between two operatives on one floor runs along that floor, it does not cross it — but `segmentCrossesPoly` answers true whenever an endpoint lies inside the polygon, so the floor counted as intervening and, being within 1" of the target, as cover. Every operative on an upper level was permanently in cover from every other operative on it. | no |
| D-071 | **The Volkus control-range visibility exemption is scoped to the features that print it, and measures HEIGHT.** | Killzones § Stronghold: "For the purposes of control range, ignore the door and parts of this terrain feature less than 2" **high** when determining visibility"; § Large Ruin says the same for its door alone. The old test was `p.z1 - p.z0 < 2`, which is the part's THICKNESS, and every extracted upper level is a zero-thickness plane — so every floor in every killzone was ignored, and an operative on a stronghold roof was in control range of the enemy standing underneath it. | no |
| D-072 | **A move is checked against enemy operatives along its whole length, and `onMovePermissions` is the seam a rule uses to be excused.** An enemy the operative was ALREADY within control range of when the move began is not one it "moves within control range of". | Core rules › Bases: "Friendly operatives can move through other friendly operatives (the base and the miniature), but not through enemy operatives." Core rules › Reposition: "It cannot move within control range of an enemy operative, unless one or more other friendly operatives are already within control range of that enemy operative, in which case it can move within control range of that enemy operative but cannot finish the move there." Both were tested only at the END of a move, and `MoveOptions.mayEnterEnemyControlRange` was declared but never read — so an operative walked over an enemy's base and through its control range unchallenged. Two team rules had noticed and written the permission off as a no-op: Gellerpox BARGE ("it can move through enemy operatives and within control range of them") and Striking Scorpions' PATIENT STALK, SUDDEN BLOW ("that operative can move within control range of enemy operatives"). Both now grant it through the hook. The already-engaged exemption is what lets a rule that hands an engaged operative a move — Tempestus Aquilons' Dash after a Charge — still work. | no |
| D-073 | **Fight re-rolls are one alternating window starting with the player who has initiative**, not the attacker's grants run to exhaustion followed by the defender's. | Core rules › Dice: "If multiple players can re-roll dice at the same time (e.g. during a Fight action), they alternate either re-rolling a dice or passing until they both pass in succession, starting with the player with initiative (this takes precedence over initiative and rules that would happen at the same time)." The two sequential steps gave the defender the last word AND a fully finalised enemy pool to look at before spending anything. The two step names `attackerRerolls` / `defenderRerolls` are kept — three team modules test against them — but now alternate rather than run in order. The unit of a turn is one re-roll GRANT, which is the engine's existing abstraction for "re-rolling a dice". | no |
| D-074 | **Torrent secondaries are checked for cover and obscured on their own; Blast secondaries inherit the primary's.** | Blast x says so and Torrent x does not: "Secondary targets are in cover and obscured if the primary target was" appears only in Blast. `ShootSequence.spread` records which rule filled the queue. | no |
| D-075 | **`MovePath.endZ` is the declared elevation of the LAST waypoint, not a post-hoc override.** | Applied after the increment loop, as it was, the climb or drop it asked for cost nothing and skipped every restriction: `{ points: [1" away], endZ: 3 }` was a free 3" ascent onto a rooftop, and a Dash could do it too, against "it cannot climb during this move". Nothing in `src/` or `tests/` sets `endZ`, so tightening it changed no caller. | no |
| D-076 | **"When jumping to a terrain feature, you can ignore its height difference of 1" or less" is charged as zero and is not a climb.** | Killzones › Jumping. `isJumpLanding` existed but only bypassed the "within 1" horizontally" guard; the very next line still charged `max(2, dz)`, so a 4" jump onto a feature 1" higher cost 6" instead of 4" — and a Dash was refused it outright, though the same paragraph says a Dash "cannot climb during this move, but it can drop and **jump**". | no |
| D-077 | **The surface an operative is standing on is not terrain it moves THROUGH, so it carries no Accessible surcharge.** | Killzones › Accessible: "Operatives can move **through** Accessible terrain … but it counts as an additional 1" to do so." Bheta-Decima gantry floors are Accessible, and `segmentCrossesPoly` answers true whenever an endpoint lies inside the polygon, so every increment along a gantry was charged the extra inch: a 3" Dash along one cost 4". Same principle as D-070. | no |
| D-078 | **Ceiling's "regardless of the operative's height" now reaches the operatives it is for.** | Killzones › Ceiling. The base-size test sat inside `if (part.z0 >= z + height)`, a branch whose arms both `continue`d — so it never changed an answer, and the operatives the rule exists for (taller than the clearance) were refused a position it explicitly allows. | no |
| D-079 | **Limited x is enforced in `weaponsOf`, and the weapon of a sequence that is still resolving is exempt.** | Appendix › Limited x: "After an operative uses this weapon x times in the battle, they no longer have it." `weaponExhausted` existed with no call site anywhere, so `weaponUses` was counted up and never read — melta bombs, demolition charges and fusion grenades could be thrown in all four turning points, and six team modules had each re-implemented the check in their own `availableWeapons` hook, hiding the hole for those teams only. `weaponsOf` is the one place Shoot's check, Fight's check, the AI's legal-intent enumeration and the UI all go through. The in-flight exemption is required, not cosmetic: `finishShoot` counts the use while the sequence is live, and every later step looks the weapon up again to read its profile. | no |
| D-080 | **Heavy's second clause is enforced against the PROFILE that was fired**, recorded on `OperativeState.weaponsUsedThisActivation`. | Appendix › Heavy: "An operative cannot use this weapon in an activation or counteraction in which it moved, **and it cannot move in an activation or counteraction in which it used this weapon**." Only the first clause existed, so shoot-and-scoot — the thing the rule exists to forbid — was legal with all 127 Heavy profiles. Keyed on the profile rather than the weapon so a weapon whose second profile is Heavy does not forbid a move because its first was used. Both halves now read `effectiveRules`, so a hook that grants or removes Heavy is visible to them. | no |
| D-081 | **Mines are checked along every increment of a move, not only where it stopped.** | `checkMines` ran once, after `op.pos` was set to the end of the path, so a Reposition straight over a mine did not fire it and the marker could be crossed again all battle. Sampled at 0.2", the same resolution as the enemy-base check (D-072). | no |
| D-082 | **Pick Up Marker asks whether the ACTIVE OPERATIVE controls the marker.** | Core rules: "Remove a marker the active operative controls." Asking only whether the TEAM controlled it — a question answered by the total APL of everyone contesting it — let any operative anywhere on the board lift a marker a team-mate was standing on, and `perform` then teleported the marker to it. `src/ai/legal.ts` carried the same filter and gets the same fix. | no |
| D-083 | **In an Approved Ops battle, `beginInitiative` is the only initiative roll-off.** | `case 'RollOff'` rolled first and then called `ctx.beginInitiative`, which rolls again: one intent consumed four dice, recorded two initiative rolls, and spent the once-per-battle roll-off modifiers (BLADES OF KHAINE Rune of Prophecy, BROOD BROTHER Mastermind) on the throw that was then discarded. Worse, the phantom roll overwrote `state.initiative`, which is what the real roll-off's tie-break reads — so a tie was decided by the discarded roll's winner rather than by who held initiative last turning point. | no |
| D-084 | **A wall vertex belongs to "the main structure" when at least ONE of its edges is a substantial run, not both.** | Killzones › Wall terrain: "only the corners and ends of Wall terrain can intervene… minor parts of the wall that protrude do not make a corner or end alone; it must be the main structure of the wall that turns a corner or ends." The test was `dist(prev,cur) < minEdge \|\| dist(cur,next) < minEdge → skip`, and every extracted wall is a rectangle 0.365" thick, so every vertex has one 0.365" edge and was discarded: `wallCornerZones` returned `[]` for all 94 wall parts across the six Gallowdark maps, `interveningParts` skipped the wall entirely, and on the two killzones where walls are essentially the only terrain **nobody was ever in cover and nobody was ever obscured**. A protruding detail still fails, because both of its edges are short. | no |
| D-085 | **`TerrainPart.opensAs` is the discriminator between a hatchway and a breach point.** | The extractor has always written it and `src/core/types.ts` never declared it, so `Operate Hatch` gated on `p.feature.kind.includes('hatch') !== false` — feature kinds are `gallowdark.wallA3`, making that `false !== false` — and `Breach` gated on a `breachWall` role that appears in no map or terrain file. **Neither action could be performed on any map**, which with the missing Tomb World access points (still open) is the second of two independent causes of a sealed board. Breach now opens the access point itself rather than hunting a sibling that does not exist, checks the breach point is within the operative's control range, and concusses only operatives on the far side of it — "each operative that's **on the other side** of the access point". | no |
| D-086 | **Declining a counteract declines that WINDOW, stamped as `opState.counteractDeclined = { player, at: activationsThisTP }`.** | `DeclineCounteract` marked every one of the declining player's operatives `counteractedThisTP`, a flag only `readyStep` clears — so passing on one window gave up every counteract for the rest of the turning point, though the only budget the rule imposes is one per operative and it is spent by counteracting. `EndActivation` already increments `activationsThisTP`, so the next window reopens by itself. The UI's "Decline — no more counteracts this turning point" label had been written around the bug and is now "Not now". | no |
| D-087 | **The On Guard window is stamped against the ACTIVATION it interrupts, not the interrupting operative.** | Killzones › On Guard: "**Once during each enemy operative's activation**, after that enemy operative performs an action, you can interrupt that activation…" The only bookkeeping was per operative (`onGuard`, `guardSpentTP`), so a three-strong overwatch net fired three free Shoots into one 2AP activation. | no |
| D-088 | **A point-blank shot is gated on control range, not raw base distance.** | Killzones › On Guard: "Target the enemy operative **within your operative's control range** (even if it wouldn't normally be a valid target)." Measuring base-to-base let an on-guard operative shoot point-blank through a Gallowdark wall — they are 0.365" thick, so almost any pair hugging opposite faces is inside 1" — and a point-blank shot also skips the Conceal-in-cover check. Control range means "visible to and within 1"", so routing through it restores the wall. The target-validity bypasses inside `checkTarget` stay: that is what "even if it wouldn't normally be a valid target" grants. | no |
| D-089 | **Hatchway Fight checks its target is within 2" of, and on the other side of, the access point.** | Killzones › Hatchway Fight: "instead select an enemy operative **within 2" of, and on the other side of**, an open hatchway's access point the active operative is touching." Neither half was checked, so it was a 1AP Fight against any enemy anywhere on the board. | no |
| D-090 | **Operative-to-operative distance is three-dimensional (`geometry.ts::bodyGap`); `baseGap` stays 2D for areas of the killzone.** | Core rules › Distances: "When measuring to and from something, do so from the closest part of it. For an operative, do so from its base… When measuring to and from **an area of the killzone**, measure the horizontal distance only (in other words, look from above to ignore the vertical distance)." The horizontal-only clause is for drop zones, territories and the centre line — not for operatives. Every operative-to-operative measurement in the kernel went through `baseGap`, which takes no elevation, so two operatives 3" apart vertically and 0.14" apart horizontally measured 0.14" apart: one on a Vantage platform was in control range of one on the floor below, could not Shoot because it was "within control range of an enemy operative", and could Fight a model standing three inches beneath it. `gapBetween` and `withinControlRange` route through `bodyGap`; Range x, the 2" cover denial and the 1" obscured exemption follow from them. | no |
| D-091 | **The turning point is scored while its effects are still live**: `endTurningPoint` emits `onEndOfTP`, then calls `scoreEndOfTurningPoint`, then expires effects. | `scoreEndOfTurningPoint` used to run after `endTurningPoint` had already returned, and `endTurningPoint` ends with `expireEffects` — so all 119 effects across `src/` with `expiry.kind === 'endOfTurningPoint'` were gone before the crit op read marker control. Marker control is decided by total contesting APL, and APL modifiers reach `aplOf` through `onStatMod` hooks that read `state.effects`, so a ploy bought precisely to win the end-of-turning-point control check expired a few lines before the check ran. | no |
| D-092 | **Reboot goes through `missionActionCheck` like every other crit-op mission action.** | It hand-rolled only the turning-point half of its conditions, so it was the one crit-op action performable while within control range of an enemy operative — up to 3VP across TP2-4, and it removed the counterplay of charging an operative to lock the marker down. | no |
| D-093 | **A Close Quarters access point is a gap in its wall: it takes the wall's thickness and the pill's length, and the wall is emitted as two bars either side.** The pill is matched to its wall at `thickness * 4`. | The hatchway/breach pill (RGB 63,57,52, 49×14px) is printed ALONGSIDE the wall it marks. The match was `thickness * 2.2` = 19.8px, and the measured perpendicular offset across all twelve CQ cards clusters at **18–22px** (118 pairs) with the nearest unrelated pairing at **73px** — so the threshold cut through the middle of the true cluster and kept 6 access points across six Gallowdark maps and **none at all** on Tomb World. And the access box was a small square at the pill's centre, which is beside the wall, so the wall ran unbroken behind it: opening a hatchway changed nothing, and `wallRouteDistance` across one was Infinity either way. Measured on the old data, an operative on gallowdark-1 reached 10" vertically on a 23.9" board. | no |
| D-094 | **A piece that is nothing but wall is template-matched on the extent of its whole run, not its largest bar.** | Splitting a wall around its access point makes "the largest bar" an accident of where the hatchway sits, so the same physical piece fitted against itself as a different one (tomb-world-1 B2 dropped to IoU 0.524). The run's extent is invariant to the split. | no |
| D-095 | **A Smoke Grenade marker must be visible to the thrower (or on Vantage terrain of a feature that is), and it lands on the surface under the point rather than at the thrower's own height.** `ActionParams.targetZ` names the level when the point has more than one. | Universal Equipment › Smoke Grenade: "Place one of your Smoke Grenade markers within 6" of this operative. It must be visible to this operative, or on Vantage terrain of a terrain feature that's visible to this operative", and "an area of smoke 1" horizontally and unlimited height vertically **from (but not below) it**". The check tested range and nothing else — `isVisible` was imported into the file and used only by the Stun Grenade — so smoke went through a 10" wall. And `perform` stamped `z: op.z`, so smoke dropped off a 4" gantry started 4" up and, since `whollyWithinSmoke` requires `b.z >= s.z0`, did nothing whatsoever to anyone standing in it. The default z is the highest surface at or **below** the thrower, because a grenade is thrown across; the Vantage clause is the case that needs `targetZ`, and it ignores only the target feature's own parts, since the rooftop is behind its own parapet. | no |
| D-096 | **Smoke softening Piercing lives in the shoot sequence (`smokeSoftensPiercing`), not in the Utility Grenades module, and it downgrades the RULE rather than adding a defence die.** | Universal Equipment: "weapons with the Piercing 2 or Piercing Crits 2 weapon rule have the Piercing 1 or Piercing Crits 1 weapon rule (respectively) instead, unless they're within 2" of each other." Modelled as `ev.count += 1` it handed back a die Piercing had never taken: `piercingValue` returns 0 for Piercing Crits with no retained critical, and the obscured step downgrades every critical to a normal before the defence is rolled — so a Piercing Crits 2 shot into smoke, the ordinary case, faced **four** defence dice. Two more bugs came with the hook's placement: it tested `dist(marker, target) <= 1` instead of the `whollyWithinSmoke` selector the obscured half of the same rule uses, and a `target.player !== b.player` guard switched the softening off whenever an operative stood in the enemy's smoke. Moving it to the sequence also fixes what dropping that guard would have broken — the module registers once per player, so two players holding Utility Grenades would each have added a die — and gives the softening to a `kind: 'smoke'` marker created by a team rule (MANDRAKES) rather than by the equipment. | no |
| D-097 | **The kill grade is two-way, and a grade lost gives the VP back** (`revokeVP` in `src/core/ops/common.ts`, newest slot first, this op's slots only). | Approved Ops › kill op: "As REANIMATED operatives are no longer incapacitated (and are therefore no longer 'kills'), your opponent's kill grade can go down during the battle (they lose VP accordingly)." `updateKillGrade` already recomputed the correct grade — `incapacitatedEnemies` drops back the moment HIEROTEK CIRCLE's `reanimate()` clears `incapacitated` and `removed` — but only ever stepped the stored grade UP, and nothing anywhere removed VP. Worth up to 2VP a game against a reanimating team, because `killGrade` is also what the end-of-battle "higher than your opponent's" point compares. Lowering the slots restores the op's 6VP cap headroom, so a grade re-reached is re-scored; nothing outside `kill.killOp` is touched, per the same paragraph's "won't retroactively change any other VPs". | no |
| D-098 | **The kill-grade row is read from `TeamState.startingSize`, not the live roster.** | Approved Ops › kill op: "The row you use is determined by the **starting** number of enemy operatives." `killOpStartingSize` counted `operativeIds` at scoring time, so a rule that adds operatives mid-battle (CANOPTEK CIRCLE 'A Ceaseless Scuttling', a STRATEGIC GAMBIT usable every turning point after the first) moved the opponent onto a harder row: a 6-strong team grown to 9 turns row 6 [1,2,4,5,6] into row 9 [2,4,5,7,9], costing the opponent a VP at four kills and possibly the end-of-battle comparison. GELLERPOX INFECTED's MUTOID VERMIN clause already relied on `startingSize` being left alone — a field the kill op never consulted. `incapacitatedEnemies` still scans the live roster, so a mid-battle addition that is later incapacitated still counts as a kill. | no |
| D-099 | **`gambitToAct` is the one selector for whose STRATEGIC GAMBIT turn it is, and the reducer enforces it.** | Core rules › STRATEGIC GAMBIT: "Starting with the player who has initiative, each player alternates either using a STRATEGIC GAMBIT or passing. The players repeat this process until they have both passed in succession." `src/core/phases.ts` used to say in a comment that "the reducer enforces no alternation", and `actorFor` in `src/ai/runner.ts` handed the initiative player every turn until they passed — so every AI-driven and soak game let one player resolve all of their gambits consecutively before the opponent could respond, and an out-of-turn `UseGambit` from the UI or a test was simply applied. Both now go through `gambitToAct`, and `PassGambit` is checked too, because passing IS the alternating turn. Four team tests used two consecutive gambits from one player; they take the opponent's turn through `yieldGambitTurn` in `tests/teams/harness.ts` rather than being exempted. | no |
| D-100 | **A granted free action is free AP, not an APL stat change — superseding D-015.** `grantFreeAction` records `ap: 1` on its `FREE_ACTION_RULE` effect and pushes nothing into `op.aplMods`; `freeApOf(state, op)` sums it, `apBudgetOf(ctx, state, op)` = `aplOf + freeApOf` is what the AP gate, the AI's search depth and the UI's activation sheet read, and `aplOf` stays the STAT that marker control totals. | Core rules › Datacards: "Regardless of how many APL STAT CHANGES an operative is affected by, the total can never be more than -1 or +1 from its normal APL. This takes precedence over all stat changes." D-015 modelled the free action as `aplMods.push(1)`, so it went through that clamp and a free action was not free: an operative already at +1 APL from a ploy gained **nothing**, and one at -1 spent the free action undoing the debuff instead of performing it in addition (audit W-38; the owner confirmed the free action should not cost against APL). The clamp is a rule about APL stat changes, and "perform a free 1AP action" is not one — it grants an action, not a stat. Two consequences fall out. Free AP now expires by itself: the effect's `endOfActivation` expiry is honoured by `expireActivationEffects`, so the nineteen team modules that each hand-rolled an upkeep popping the +1 back out of `aplMods` — because, as their comments said, "the engine never pops it" — lose that half of their upkeep. And two separate grants now give two free AP rather than one, which is what the printed rules say and what the clamp was silently preventing. | no |
| D-101 | **A stronghold's traced wall ring is banded, not extruded to the piece maximum: each bar stops 1" above the highest floor of its own feature that it BORDERS.** On Stronghold A that last inch is a `rampart` part typed `['Insignificant','Exposed']`; on Stronghold B it is Heavy. `volkus.stronghold.parapet` = 1.0", confidence `assumed`, **owner-confirmed 2026-08-23**. | Killzones § Stronghold A: "The upper level(s) of a stronghold terrain feature is Ceiling and Vantage terrain" — and § Vantage is what makes climbing one worth doing. The cards trace the ring as one piece of ink with no height of its own, so every wall part was extruded to the PIECE maximum (5.906" / 7.48") while the Vantage floors it encloses sit at 3.0" and 6.0": the edge of every upper level was a 2.9"/4.5" opaque Heavy parapet. Measured on the shipped data, the best spot on volkus-1 Stronghold A's roof saw **31 of 568** killzone-floor positions (5%); across all six maps every one of the eighteen stronghold levels sat between 4% and 18%. Climbing the two biggest features in the killzone was a pure downside and Vantage's Accurate 1/2 was unreachable from them. Three things had to be right. **Banding per bar, not per feature**: Stronghold B's second level is a small square in one corner, so capping the whole ring at that level's parapet leaves its big lower level in a 4" well exactly as before (17% → 19%). **Cutting each bar where the upper floor's extent ends**, so the corner gets its parapet and the rest of that side gets its own. And **ramparts only on Stronghold A**: § Stronghold F says "The small broken ramparts on the edge of the Vantage terrain of Stronghold A are Insignificant and Exposed terrain", while the section ends "All other parts of it are Heavy terrain" — so A's roof edge stops blocking (`defaultBlocksVisibility` already returns false for Insignificant + Exposed) and B's does not. Result: Stronghold A 48–68%, Stronghold B's top level 49–61%, its lower level 21–48%. The `*.top` heights stay in the catalogue because they describe the promethium tank and the tower, which the cards do not draw and the extraction never emits. | **yes — 1" is the owner's number, not a measured one; the same convention as `volkus.largeRuin.top`** |
| D-102 | **Both base-overlap guards in `validateMove` use `basesOverlap`, closing D-050.** The end-of-move check and the move-through-an-enemy's-base check inside `enemyOnTheWay` were both spelled `baseGap(...) < -1e-4`; seven more dead copies in team modules are converted with them. `tests/teams/harness.ts` gains `chargeTo(ctx, state, moverId, enemyId)`, which binary-searches the approach line for the point at which the bases just touch. | Core rules › Bases: "The sides of different bases can touch, but a base cannot be placed on another", and "Friendly operatives can move through other friendly operatives (the base and the miniature), but not through enemy operatives." `baseGap` returns `Math.max(0, …)` on both of its branches, so `< -1e-4` could never be true and **neither guard had ever fired** — D-050 recorded the first and missed the second, which is the base half of D-072 and the reason `MoveOptions.mayMoveThroughEnemies` had never had anything to permit. The owner's call was "fix and nudge". Nudging turned out to mean 32 tests across 14 files, in two shapes: a Charge ending inside the enemy's base, and a setup PLACING two operatives inside each other so they would start engaged. Both are the rule being broken rather than the tests being unlucky, so every one was moved to touching rather than exempted; `chargeTo` computes that point from the two bases instead of by hand, because oval bases are ellipses with a facing and `rA + rB` is wrong for them. Three fixtures needed a route rather than a nudge — SLITHERING EVASION, PATIENT STALK and CLEAVER FLURRY all print permission to move within an enemy's control RANGE, which is not permission to walk over the model. Two probe boards (the Fellgor and Gellerpox D-026 honesty tests) had placed every operative 1.6" apart on 40mm bases, which is 0.025" of clearance, and could no longer offer their carve-out Charges a legal path at all. And the Fellgor volkus-1 soak was re-seeded from 4242 to 4245: the perturbed game played out with one fewer Shoot and nobody died, which makes that test's Frenzy assertions vacuous rather than wrong — the seed is a property of the game, not of the rule under test. Seven further sites spelled the same rule as a raw subtraction (`dist − rA − rB < -1e-4`). Those DID fire, so they were left out of that change and folded in afterwards (see the note on this row): `baseRadius` circumscribes an oval, so against a 75×42mm base they over-rejected — refusing legal placements rather than allowing illegal ones. All seven now use `basesOverlap`, and no copy of the rule remains outside it. | no |
| D-103 | **`actionTargetOptions(ctx, state, op, def)` is the core selector for "what could this action be aimed at", and `actionAvailability` consults it for the `marker`, `markerChoice` and `part` kinds.** The UI's new `firefight.aim` branch renders whatever it returns; one legal option dispatches straight through with no screen. | Approved Ops › Play the battle: "Score VP by performing mission actions and controlling objective markers." Every crit-op mission action takes a `markerId`, and `actionAvailability` returned early on `needsTarget` without ever asking whether any marker would do — so `play.tsx` rendered the row `disabled={!ok}` with `ok` decided by `def.check(ctx, state, op, {})`, whose reason is always "select an objective marker". The row was permanently disabled and nothing could aim it, so five of the nine crit ops scored **0VP for a human player for the whole battle** (W-05). The selector lives in the core because which markers an action will accept is a rules fact, not a UI one — Download refuses your own objectives, Reboot wants an inert marker, Pick Up Intelligence an `intelligence` marker — and docs/UI.md forbids the UI re-implementing a core selector. Consulting it from `actionAvailability` is what stops the new ids becoming *enabled* dead buttons, which is the mirror-image bug Pick Up Marker had shown all along: offered while the operative carried nothing and stood 12" from every marker, and one tap wrote "no such marker" into `state.rejected`. Two details the adversarial verifier caught and that are pinned by tests: the `markerChoice` branch must return the bare form as the WHOLE option set on the leg where the rule gives no choice ("If a player's objective marker has it, move it to the centre objective marker" — no "(your choice)" clause), because `Move Orb`'s check ignores a `choice` param there and enumerating markers would offer three buttons that all do the same thing; and the no-target reason must come from the nearest real candidate, not from `check({})`. `src/ai/legal.ts` seeds the same options so the bot reaches these actions too, with the bare `{}` still last. | no |
| D-104 | **DOOR FIGHT is registered in `src/core/actions.ts`, gated on `killzone === 'volkus'` AND the data actually holding a `role: 'door'` part** — the same shape as the Close Quarters actions, which gate on an access point existing. | Killzones § Cityfight: "DOOR FIGHT 1AP… instead select an enemy operative on the killzone floor and within 2" of, and on the other side of, a door the active operative is touching… This action is treated as a Fight action." The audit filed this as "one operative in a doorway seals every building on Volkus". **That cause is disproved.** The door's control-range exemption (killzones.txt:266) IS implemented — `controlRangeIgnores` returns true for a `role: 'door'` part on the stronghold and large-ruin kinds — so an enemy just inside a doorway is in control range and can be Fought normally, and the doors are Accessible and not solid, so operatives walk through them. Stronghold A's doorway is 1.167" clear against a 1.26" 32mm base, so nothing can stand IN it at all. The real gap is exactly what the rule's own note describes: an enemy on the far side, beyond control range but within 2" of the door, could not be attacked by anything. The gate matters as much as the action: the audit proposed `killzone === 'volkus'` alone, and `tests/fixtures.ts testMap()` is killzone 'volkus' with no terrain, so that predicate would have added a Door Fight row to every synthetic fixture in the suite and handed the AI a candidate to probe on every board. Placement is provisional: if the killzone-module registry W-29 proposes is adopted, Door Fight moves there. | no |
| D-105 | **`TerrainPart.maxOperatives` is read by one exported helper, `occupancyCapExceeded`, which every placement path calls: `validateMove`, `canDeployAt`, and the two team set-up-again paths (WARPCOVEN Temporal Flux, TEMPESTUS AQUILONS DROP INSERTION).** | Killzones § Stronghold H: "You cannot have more than one friendly operative on the highest upper level of Stronghold B at once… (this means an enemy operative cannot be prevented from moving onto **or being set up on** the other side)." The extractor has stamped `maxOperatives: 1` on that level since the maps were built and `grep -rn maxOperatives src/` returned nothing — the cap was data-only. Two things the rule's own parenthesis settles: it is per PLAYER, so an enemy may share the level; and it binds every way an operative arrives, which is why this is one helper rather than a test repeated per site — the adversarial verifier found the third and fourth sites, two private re-implementations of placement legality in team modules that a two-site fix would have left silently violable. `index.capped` is empty on all eighteen non-Volkus maps and every synthetic fixture, so the check costs nothing where it does not apply — which matters because `reachableCells` calls `validateMove` once per grid cell. **Deferred, and recorded rather than guessed:** the rest of § Stronghold H — "must be placed on one side or the other of that level, it cannot be placed in the middle" and the oversized-base fallback. The plate measures 2.04" × 2.15", so a 32mm base (1.26") does not fit inside either ~1.02" half, which makes the rules' oversized-base case the NORMAL one on this geometry rather than the exception. Splitting the plate needs a division the cards do not print, so it wants the same provenance + confidence treatment D-101's parapet got. | **yes — the side division is an owner call, see docs/RULES-AUDIT-PLANS.md W-31** |
| D-106 | **A counteraction has an ACTION ALLOWANCE, asked of the `counteractActions` hook at the moment of each action; the constraints that come with a granted extra action belong to the rule that grants it, through `canPerformAction`.** | Core rules › Counteract: "you can select one of their expended operatives with an Engage order to perform a **1AP action (excluding Guard) for free**" — one action, which stays the default. DEATHWATCH's VETERAN ASTARTES prints the exception: "Whenever it does, it can perform an **additional 1AP action for free** during that counteraction, but both actions must be different and you cannot perform a Fight and Shoot action during the same counteraction." `reducer.ts` capped on `actionsUsed > 0` with no seam, so the grant had nowhere to live. Free AP is not the seam either — a counteraction forces `ap = 0` and skips the budget check entirely, which is why ANGEL OF DEATH's WRATH OF VENGEANCE, written through `grantFreeAction`, had been inert since it was added. A new hook name rather than a field on `onCounteract`, which is an eligibility query emitted per candidate on every render and already mutated for other purposes; asked lazily, so a ploy used mid-counteraction is seen. The constraints stay with the granting rule because the kernel deliberately does the opposite — "Counteracting isn't an activation… it means action restrictions won't apply" — so "both actions must be different" is this rule's own condition, not the universal restriction returning. Actions are compared on `treatedAs ?? id`, so a team's own `Fight (…)` variant is not a second different action; the Phase Sweep chain, which deliberately carries no `treatedAs` so each link gets its own restriction key, is named explicitly. Three things had to be fixed with it, none reachable until a counteraction could have two actions: the 2" cap is on the COUNTERACTION and was applied per action (Reposition 2" then Dash 2" moved 4"), so it is now spent against `counteractMoveLeft`, which the reducer, the AI and the command sheet all read; `Shoot (Veteran Astartes)` and `Fight (Veteran Astartes)` key off `actionsThisActivation`, which a counteraction repopulates from empty, and `apSpent` stays 0 throughout one, so both became legal there and would have made a THIRD action; and WRATH OF VENGEANCE had no `usable` guard, so played outside a counteraction — where its free-AP effect is not inert — it really did hand out a fourth AP. | no |
| D-107 | **`reachableCells` expands every level `surfacesAt` offers at a neighbour, and a step refused by a climbable solid part emits one climb-over macro edge carrying the top it crosses (`ReachCell.via`), which `routePath` turns into the rules' up / across / down increments.** | Killzones › Terrain and Movement: "Operatives cannot move through terrain — they must move around, **climb over** or drop/jump off it", and the worked example at killzones.txt:182: "The operative moves up for 2\" … until it's above the highest point it must climb over. It moves across 3\" until its base is fully past the terrain feature, then drops down for 0\"." `validateMove` has always accepted that path — hand-built over a real 2" Volkus wall it comes back ok at 4" of a 6" budget. Nothing could ever propose it: the fill gave each cell ONE elevation, `closestSurface(np, cur.z)`, which returns the level nearest the operative's feet while `surfacesAt` always offers 0 — so from the floor the answer was always 0, the climb arithmetic was dead code, and **no cell above the floor existed in the field on any of the 24 shipped maps**. That is two defects and the larger is not about walls: every Vantage level in the game was unreachable through the move preview and the AI. The wall needs the macro edge on top of the level fix, because a wall top is not a level anything may stand on (killzones.txt:205) and the far side is three lattice cells away; the top is therefore never a cell, only a waypoint. How far "across" reaches is measured by intersecting the ray with the part's own outline, not its bounding box — for a diagonal step over a 0.2"-thick, 5.5"-long wall the box's extent along the ray is the wall's LENGTH. Gallowdark is deliberately untouched: its walls are 2.362", inside the 3" climb reach, and would all have become climbable, so the `Wall` type is checked first ("Operatives cannot move over or through Wall terrain"). Volkus parts at 3.5", 4" and 7" stay refused by the 3" reach — they need the Accessible door. Two consequences came with it: `src/ai/moves.ts` deduped candidates on `x,y` with no `z`, silently dropping the elevated one the moment the field had two levels; and the fill re-sorted its whole queue on every pop, O(n² log n), which is now a binary heap — worst AI decision 246ms before the change and 237/231ms after, against a 300ms box, with a field half again as large. | no |
| D-108 | **Close-quarters walls are extracted from the printed CONNECTOR BLOCKS, and a block is a PART of the wall piece whose end lands on it — never a terrain feature of its own.** The Gallowdark / Tomb World cards draw a run as a 9px bar and a 27px square block wherever a physical wall piece ends; both hold on all 211 blocks and every bar across the twelve cards, so both are exact fractions of a lattice square (1.09508" and 0.36503"). `cq_features` now finds the blocks (erode the wall mask by more than a bar width), chains collinear ones joined by a bar, and picks the tiling that consumes every printed letter exactly once. A block with a grey ✕ knocked out of it is a **WALL END** (`keys/TW3.jpg`), thirteen of them; geometrically identical, kept as its own `role` because a player setting the killzone up places a different sprue part. | The owner's report was "their corners [should] be square just like in the original maps". They were not: two bars meeting at a right angle cover only half the square where their centrelines cross, so a 0.183" × 0.183" quarter-block of Wall terrain was missing at every L-corner — **30 of them across the twelve maps** — which is a hole in a sealed room, not a drawing artefact. Three more things fall out of reading the blocks rather than the lattice, and each is a bug the old method could not see. (a) **The tiling stops being a guess.** The block set is an independent measurement of where the pieces end, and it disagreed with the lattice tiling on exactly the two cards already known bad: `tomb-world-1` (whose A2 was the only entry ever allow-listed for the CQ tiler, and whose `B?` wall no OCR could place) and `gallowdark-2`. Every wall piece on all twelve cards now carries exactly one printed letter, no letter is left over, and every card's multiset is inside the printed killzone inventory — a property nothing else in the pipeline can assert. The `IOU_ALLOW` entry is deleted, and `validate_maps.py`'s own honesty gate would fail if it were not. (b) **Two pieces are printed off the lattice.** `tomb-world-1`'s A2 and `tomb-world-6`'s B4 sit offset by exactly half a square; the lattice sampler snapped them onto a line, moving one 1.9" and re-sizing the other. Block centres snap to the nearest HALF step, which absorbs the 2–3px by which the cards shift a perimeter block (the border strip is drawn compressed) without touching a real half-square offset. G8 is widened to the half step for the same reason. (c) **The ✕ was invisible to the old detector**, because it punches an 84px hole in the wall colour at the node — a probe reading raw ink finds nothing there. It is healed back into the mask before blocks are found; the one-pixel dashed centre line printed in the same grey is rejected on size. **Why a block is not its own feature.** It is its own sprue part and that was the first implementation, but a `TerrainFeature` is what the rules count: *"An operative cannot be in cover from and obscured by the same terrain feature"* is keyed on the feature id (`visibility.ts`), and a post emitted as its own feature hands the defender cover from the post AND obscured by the very wall bar it caps — one printed wall piece paying out both. It is folded into the piece whose end lands on it (the first in tiling order; a post standing mid-run, which `gallowdark-2` prints once, goes to the piece it stands in). Two further consequences of the discarded model, both now avoided: `wrecka-krew`'s BREAK STUFF picks the nearest part's FEATURE and would have broken a 1.1" post instead of the wall run, and `docs/MAPS.md`'s feature counts would have doubled. Template fitting is unaffected either way — `_footprint` matches a CQ piece on the extent of its bars and ignores the joint roles. **What genuinely changes in play**, and is accepted rather than worked around: a post stands 0.365" proud of each wall face and 0.548" past a free end, so it is nearer than the bar. Cover, obscured and the dozen team rules that gate on *"Heavy terrain within 1\""* (BLOODED, RATLINGS, NEMESIS CLAW, FARSTALKER, HIEROTEK, KOMMANDOS, MANDRAKES) all read slightly more permissively on close-quarters maps, and EXODITE's *"more than 2\" tall"* test slightly less. That is the terrain the card draws and the kit builds; the previous answers were wrong by a missing post, not right by a stricter rule. `wallRouteDistance` gained a bounding-box reject in the same change because it is O(W³) and W grows by half. | no |
| D-109 | **A hatchway is drawn as a door and aimed at on the board.** `Board.tsx` renders `role: 'accessPoint'` and Volkus `role: 'door'` parts with their own vocabulary — a closed access point is a panel with a lock, an open one is a gap between two jambs with a dashed threshold, amber for a 1AP Operate Hatch and red for a 2AP Breach — and `ArmedState` gains `onPart`, so `firefight.aim` arms the board for the two actions aimed at terrain instead of listing part ids. | The owner's report was that Gallowdark and Tomb World *"don't have clearly outlined doors / hatches to use"*, and it was two problems wearing one coat. **Visually**, `fillFor` keys only on terrain TYPES, and `effectivePart` rewrites a closed access point to `['Heavy','Wall']` — byte-identical to the wall it is cut into — so all sixteen hatchways on a board were invisible; a Volkus doorway is `['Accessible','Heavy']` and was lost to the same green. Every abutting wall part was also stroked separately, so a continuous run read as a row of bricks with gaps: the close-quarters wall family is now stroked once as a silhouette and then filled, which is what leaves a corner square instead of crossed. **Functionally**, the actions were reachable but unusable: the aim list read `access point gallowdark-1.A3-1.access`, which does not say which of the sixteen it is. `accessPointLabel` names it by the wall it is cut into, its state and its distance. Four rules bugs surfaced while wiring it and are fixed with it. (1) `Operate Hatch.check` never read `opensAs`, so on Tomb World — where A1 and B2 carry BREACH POINTS — the list offered every breach point as a **1AP** Operate Hatch and the reducer opened it; BATTLECLADE's REMOTE ACCESS and CANOPTEK CIRCLE's OBELISK NODE CONTROL delegate to it and had the same hole. (2) Control range was measured to a synthetic 20mm marker at the access point's bounding-box CENTRE. A hatchway is ~2" wide, so an operative standing at the edge of a doorway, plainly within 1" of it, measured 1.2" away and was refused; `accessPointGap` measures to the polygon, and all seven sites use it — including `Breach.perform`'s concussion roll, which otherwise disagreed with its own `check` by up to 0.9" about which operatives are behind the door. (3) `NEEDS_TARGET` had no entry for DOOR FIGHT, so the sheet dispatched it bare and `check` refused it for want of a target: a permanently disabled button on every Volkus map. `AIMED_KINDS` gains `'operative'` so it and HATCHWAY FIGHT can be aimed at all. (4) `src/ai/moves.ts` keyed its reachability-field cache on `Object.keys(state.terrainState).length` — a COUNT. `Operate Hatch` writes the same key on open and on close, so once a hatchway had been opened the count never changed again and CLOSING one returned the field computed while it was open; the AI then proposed a move through a shut door and the reducer wrote it to `state.rejected`, the one thing both soak suites assert is empty. It was unreachable until this change made hatchways usable. The door's tap disc is capped at half the distance to its nearest neighbour: the 44px thumb minimum is 3.1" of board at fit zoom on a 390px phone, and the two closest access points in the shipped data are 2.5" apart, so uncapped discs overlapped and SVG paint order decided which hatchway a tap opened — for 1AP, with no confirm step. | no |
