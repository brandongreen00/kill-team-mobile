# Team status

`done` requires: data pinned by tests · selection rules enforced · faction rules, 4 strategy ploys,
4 firefight ploys, 4 equipment, unique actions and abilities implemented as hooks with tests ·
rare weapon rules implemented · a bot-vs-bot soak game on two maps with zero rejected intents.

Every module lives in `src/teams/<slug>/index.ts`, is registered in `src/teams/index.ts`, and reads
its printed rule text from `data/teams/<slug>.json` (never retyped). Selection rules are one shared,
data-driven validator: `src/teams/selection.ts`.

| team | data | selection | faction rules | strategy ploys | firefight ploys | equipment | unique actions | abilities | rare rules | tests | AI game | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **kasrkin** | pinned | ✅ shared validator | 2/2 | 4/4 | 4/4 | 4/4 | 4/4 | 7/9 | ConcealedPosition ✅ | 26 | ✅ 2 maps | Skill at Arms is 4 gambit options (never picked silently) + Veteran Leadership's second pick. Rapid Fire is a second `Shoot (Rapid Fire)` action. Melta Mine / Proximity Mine are reminder-only — they need a carried-marker mine the Pick Up/Place Marker actions can own (engine gap, see notes below). |
| **angel-of-death** | pinned | ✅ shared validator | 2/2 | 4/4 | 4/4 | 4/4 | 1/1 | 5/8 | — | 17 | ✅ 2 maps | All six CHAPTER TACTICS bound; chosen through `setChapterTactics` (Select Operatives has no decision channel — nothing applies until chosen). Astartes is two extra actions (`Shoot (Astartes)` / `Fight (Astartes)`). Heroic Leader and Doctrine Warfare (CP discounts on a ploy you *would* use) are reminder-only: the reducer charges CP before any hook sees the ploy, so a discount can only be modelled as a refund, and both are conditional on a choice made before the ploy is paid for. |
| **plague-marines** | pinned | ✅ shared validator | 3/3 | 4/4 | 4/4 | 4/4 | 3/3 | 7/8 | Poison ✅ Toxic ✅ PSYCHIC ✅ | 18 | ✅ 2 maps | Poison tokens are per-player effects on the bearer; Disgustingly Resilient rolls through the injected RNG. Icon of Contagion (Contagion free inside enemy territory) is reminder-only — same CP-before-hook ordering as above. |
| **imperial-navy-breacher** | pinned | ✅ shared validator | 2/2 | 4/4 | 4/4 | 4/4 | 7/7 | 10/12 | Shield ✅ Detonate ⚠ partial | 20 | ✅ 2 maps | Breach and Clear records the paired operative as an effect; the UI/AI reads it to offer the back-to-back activation. Detonate enforces "cannot be selected if your GHEISTSKULL isn't in the killzone" but its "shoot everything within 2\" of the marker instead of a valid target" needs a target-selection seam. Expendable (op scoring exclusions) is reminder-only: it belongs to `src/core/ops/**`, owned by another agent. |
| **celestian-insidiants** | pinned | ✅ shared validator | 4/4 | 4/4 | 4/4 | 4/4 | 3/3 | 12/12 | AntiPSYKER ✅ Shield ✅ | 35 | ✅ 2 maps | INSPIRING and the four BENEDICTIONs are effects on the operative; Martyrdom raises a real BENEDICTION decision (operative × benediction) through `decisionHandlers`, and VOCIFERA MORTIS widens its candidate list once per battle. HOLY DEFENDER and the Zealous Ultimatum use the two new seams (`onSelectTarget`, a decision the OPPONENT answers). Bladed Stance is partial — it reorders the fight so the retaliating MORTISANCTUS resolves first, but the engine generates the strike/block options so "that success must be used to block" cannot be enforced. Two clauses are reminder-only: PSYCHIC actions' "friendly CI operatives cannot be selected" (a team cannot reach into another team's unique-action target selection) and HOLY DEFENDER's Fight half (only the Shoot step has a target-substitution seam). |
| **kommandos** | pinned | ✅ shared validator | 1/1 | 4/4 | 4/4 | 4/4 | 5/5 | 9/10 | ConcealedPosition ✅ Explosive ✅ | 28 | ✅ 2 maps | Throat Slittas is its own `Charge (Throat Slittas)` action (`treatedAs: 'Charge'`) because the universal Charge refuses a Conceal order and `canPerformAction` can only forbid; Krumpin' Time and Explosive are registered the same way (the Explosive shot targets the BOMB SQUIG itself through the point-blank path, with its Hit penalty cancelled). Taktical Wot-notz delegates to the universal grenade actions and restores the kill team's grenade uses afterwards ("doesn't count towards their action limits"). Boom! rolls its 2D6 and resolves the explosion as direct Blast damage — the engine has no "free action on death" seam (`onIncapacitated.freeActions` is declared but never consumed). BREACH places its marker and takes the AP discount; its "treats … as Accessible terrain" grant is reminder-only (terrain types are compiled from the map, with no runtime seam). Expendable is reminder-only: it is op scoring, owned by `src/core/ops/**`. |
| **pathfinders** | pinned | ✅ shared validator | 1/1 | 4/4 | 4/4 | 4/4 | 15/15 | 18/19 | — (none printed) | 37 | ✅ 2 maps | Markerlight tokens are one pure count (`markerlightTokens`) that every band reads: 1 → Saturate + Balanced, 2 → +1 Hit via `onStatMod` (capped at 3+), 3 → clears `seq.obscured`, 4 → Seek Light; they are shed at the end of an enemy activation in which it moved. SAVIOUR PROTOCOLS redirects through `onSelectTarget` and SUPPORTING FIRE through the new `ignoreFriendlyControlRange` flag. Group Activation is partial — the pairing is recorded as an effect for the UI/AI, because the engine alternates activations strictly (same as the Breachers' Breach and Clear). POINT-BLANK FUSILLADE swaps in a melee copy of a ranged weapon and hands the defender the first dice; its "you cannot block" clause is unenforceable (the engine generates the strike/block options). |
| **hierotek-circle** | pinned | ✅ shared validator | 3/3 | 4/4 | 4/4 | 4/4 | 16/16 | 6/6 | Magnify ✅ | 33 | ✅ 2 maps | Reanimation Protocols leaves a marker (clearing the operative's tokens and effects), and the Ready step raises a real decision — marker × order — that rolls, revives on a 3+ with 1 wound and re-offers a different marker on a 1-2. A REANIMATED operative keeps its id, so the **Martyrs tac op cannot score twice for it** (pinned by a test), and Living Metal still heals it because it resolves "after all other rules in this step". Magnify uses the new `onValidTarget.viewFrom` proxy seam (visibility, cover and obscured from the APPRENTEK/CRYPTEK; range stays the shooter's) plus Ceaseless. Not implemented: REANIMATE's "if you spent 1 additional AP" automatic mode (the printed 2AP roll is), and REANIMATED FUNCTION's "no effect for the Martyrs tac op" carve-out — `onMarkerControl` carries no caller, so ordinary control and op scoring cannot be told apart. VISION OF MADNESS is modelled as −1 APL on the activation it interrupts: the engine has no way to refuse an activation. |

### Batch 2 — Astartes and Heretic Astartes

| team | data | selection | faction rules | strategy ploys | firefight ploys | equipment | unique actions | abilities | rare rules | tests | AI game | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **scout-squad** | pinned | ✅ shared validator | 1/1 | 4/4 | 4/4 | 4/4 | 3/3 | 7/7 | — (none printed) | 37 | ✅ 2 maps | Forward Scouting's six selections go through the pure setter `selectForwardScouting` (D-017's shape — Set Up Operatives has no decision channel); Devise Plan, Spy, Redeploy, Reposition, Trip Alarm, Designate Target and the two gambits all resolve. Astartes is the `Shoot (Scout Astartes)` / `Fight (Scout Astartes)` pair, and its "can counteract regardless of its order" is **live** on the SERGEANT datacard (the clause is printed there, not on a faction rule) through the widened `onCounteract` seam. Booby Trap is partial: the 2D3 lands but springs at the activation boundary, because `checkMines` is core-only and `applyMove` emits no marker-trigger hook, so "end its action" cannot be honoured. Designate Target is exact when shooting; its fighting/retaliating half is modelled as Balanced, which adds nothing when Balanced is already present (`fight.ts` emits no `onRollAttack`). Grapnel Launcher is reminder-only (no seam changes a climb's charged vertical distance); Grapnel Assault is partial (climbs and drops are detected as an elevation change across the activation; a level jump and "moves underneath Vantage terrain" are invisible). SNIPER Camo Cloak takes the "additional cover save" branch deterministically; TARGETING OCULARS auto-fires up to twice per TP under D-022. |
| **murderwing** | pinned | ✅ shared validator | 2/3 (+1 partial) | 4/4 | 3/4 (+1 partial) | 4/4 | 4/4 (+3 from equipment) | 9/10 | — (none printed) | 57 | ✅ 2 maps | The BOOST is three sibling `ActionDef`s — `Reposition (BOOST)` / `Fall Back (BOOST)` / `Charge (BOOST)`, each `treatedAs` its universal action (D-021) — because `validateMove` has no per-increment seam and `onMoveRules` is never emitted. Each charges the boosted inches against the same allowance and the Charge variant drops the printed +2"; the swept area is stored as an effect, which is what gives BOOST ZONE a real definition. **Jump Pack is partial**: the BOOST *replaces* the move action rather than being one increment of it, so "at the start of any straight-line increment" and "it can continue moving after a BOOST" are not modelled. Astartes is complete — two extra actions plus the now-live counteract clause. **MALICIOUS NARCISSISM is partial**: the "cannot counteract until that operative is expended" half is enforced through `onCounteract` (bound at a lower priority than the Astartes widening, so the veto still wins); "you can skip your activations" needs an activation-order seam. **SLICE THE VEIL is reminder-only** — there is no reserve/off-killzone state and no decision channel at deployment, and every clause hangs off that first choice. MURDEROUS DESCENT refunds the CP and un-marks itself when no legal Charge exists; its close-quarters "different room" variant is reminder-only (rooms are not modelled). Path to Damnation attempts a Boon on a deterministic, logged policy; Chaos Champion takes its Challenge target from the gambit's `data` with a logged default (D-016). |
| **phobos-strike-team** | pinned | ✅ shared validator | 4/4 | 4/4 | 3/4 | 3/4 | 2/2 | 10/18 | Custom ✅ Detonate ✅ | 41 | ✅ 2 maps | The Explosives and Haywire Mine markers are **real carried markers** created at deploy, each with its own Pick Up / Place action, so "cannot be placed within an enemy operative's control range" and the free Dash on placement are enforceable — **this closes the Kasrkin carried-marker-mine gap**. Detonate is fully implemented: the shot goes through the point-blank path with its Hit penalty cancelled, victims within 2" of the marker are queued exactly as Blast secondaries (Heavy terrain wholly intervening excluded, cover and obscured cleared), and the marker is removed; only "in an order of your choice" is deterministic. Custom is a pure setter (`setCustomWeaponRules`, D-017). Astartes is complete, counteract clause included. Reminder-only or partial: Omni-Scrambler's "cannot be activated" (no activation-refusal seam — the action ban is enforced and is stricter); Tactical Advantage's initiative re-roll (`rerollOffered` is never consulted) and its Command Re-roll discount (Command Re-roll never emits `onPloyUsed`); Medic!'s "must end that move within this operative's control range"; Track Target's guard state; Proximity Mine's "end its action"; Grav-chute and Grapnel Launcher on both REIVERs (`onMoveRules` is never emitted); STEALTH ASSAULT's extra dice resolution; PURITY SEALS' fighting/retaliating half (`onRollAttack` is shoot-only). |
| **deathwatch** | pinned | ✅ shared validator (leader `inList`) | 1/2 (+1 partial) | 4/4 | 4/4 | 3/4 | 0/0 | 9/13 | PhaseSweep ✅ Shield ✅ | 40 | ✅ 2 maps | Veteran Astartes' two Shoot / two Fight actions are two extra `ActionDef`s that also charge the printed "1 additional AP" surcharge, and "can counteract regardless of its order" is now **live**; its other counteraction clause — the extra free 1AP action during a counteraction — stays reminder-only, because the reducer hard-codes one action per counteraction with no hook. Special Issue Ammunition is a `Shoot (Special Issue Ammunition)` action whose six ammunition types are parsed out of the printed rule text; AMMUNITION RESERVE widens its once-per-TP allowance to two different rules, once per battle. Phase Sweep is a four-link chain of its own `Fight (Phase Sweep …)` actions, each refusing an enemy already swept this activation. Adaptive Swordsmanship's "that success must be used to block" is unenforceable (the engine builds the strike/block options) and Advanced Omni-Scrambler's "cannot be activated" is unenforceable; both are implemented for everything else. Reminder-only: Adaptable Armoury (the reducer caps equipment at four before any hook), Grav-chute and Grapnel Launcher, and the Scrutavore Servo-Thrall's mission-action exemption (`canPerformAction` can only forbid). |
| **wolf-scouts** | pinned | ✅ shared validator | 1/2 (+1 partial) | 4/4 | 4/4 | 4/4 | 2/3 | 7/11 | PSYCHIC ✅ (keyword only) | 40 | ✅ 2 maps | Everything hangs off one per-player `wolf-scouts.storm.<player>` marker: Elemental Storm places it as a 0CP gambit, "within your STORM" is base-to-marker ≤6" and "wholly within" is `dist + baseRadius − markerRadius ≤ 6`, and eight other rules read it. Conceal-order charges are two extra `treatedAs: 'Charge'` actions. Hunting Astartes' second Shoot is **two** actions — 1AP and a 2AP plasma variant — because `onActionCost` cannot see the weapon, so the printed "1 additional AP if both actions use a plasma gun or plasma pistol" is carried by the action's own AP. **Hunting Astartes is still partial**: "can counteract regardless of its order" is now live, and "change its order instead of performing an action" is a 1AP `Change Order (Hunting Astartes)` gated to counteracting inside the STORM, but "you can change its order **first**" (and then still act) has no seam. Grizzled Veteran is complete. Spiritual Chirurgy cancels the injured penalties through `onStatMod` and survives the FANGBEARER's death. Cast the Runes banks CP and discounts the Command Re-roll — **shoot attack and defence only**, since `fight.ts` offers its re-rolls without emitting any hook. Haywire Mine is partial and **Proximity Mine (the detonation) is reminder-only**: `checkMines` lives inside `applyMove` and emits no hook. |
| **legionary** | pinned | ✅ shared validator | 2/2 | 4/4 | 4/4 | 3/4 | 1/1 | 10/11 | PSYCHIC ✅ (tag only) SiphonLife ✅ | 44 | ✅ 2 maps × 2 equipment sets | Marks of Chaos is a pure setter (`setMarkOfChaos`, D-017) and nothing applies until it is called; all five keywords bound. Astartes is complete — two extra actions plus the now-live counteract clause. CHAOS TALISMANS is **partial**: it raises a real PendingDecision from `onRollAttack` that discards a fail, retains another as a normal success and inflicts the D3 — but only when shooting, because `fight.ts` emits no post-roll hook. Devastating Onslaught is partial: enemies cannot assist and the free Charge is D-015 extra AP capped at 2", but "must end that move within control range of *that selected* operative" has no seam. Daemonic Aura pre-rolls its D6 at `onActivationStart` (rolling inside `canPerformAction` would burn RNG outside the reducer), so it does not cover a counteraction. Unleash Daemon and Infernal Pact are real PendingDecisions answered through `decisionHandlers`, with "Not yet" first so the deterministic default is to decline. UNENDING BLOODSHED is paid for during the fight and pays out inside `onIncapacitated` — the effect is exact, the moment of purchase is shifted. GRISLY MARK places its marker as printed, but **the JSON gives that marker no effect at all** — see the data problems below. |
| **nemesis-claw** | pinned | ✅ shared validator | 2/2 | 3/4 | 3/4 | 2/4 | 3/3 | 8/9 | Terrorchem ✅ | 37 | ✅ 2 maps | Astartes is two extra actions (the second gated on a bolt pistol / boltgun / scoped bolt pistol having been selected for one of them) plus the now-live counteract clause. In Midnight Clad sets `seq.obscured` at `onCollectAttackDice`, which still precedes the retention and obscured-discard steps. Terrorchem tokens are per-player effects on the bearer; the vial tokens through Devastating alone because its Critical Dmg is 0, and the D3-on-activation half is printed under a **mis-titled "POISON OBJECTIVE" ability** (data bug below). Screecher strips Balanced/Ceaseless/Relentless from enemy weapons at priority 45 and clears every shooting re-roll grant; only a Command Re-roll spent in a **fight** survives, because `fight.ts` never emits `onRollAttack`. PREYSIGHT auto-uses only when the target is already within 6", where its Range 6" half costs nothing. RETURN TO DARKNESS grants the free action and the 4" cap; its "must end with Heavy terrain within control range / underneath Vantage" and "cannot end closer to enemy operatives" clauses are reminder-only (no hook constrains a move's end position). VOX SCREAM's "cannot activate it during this activation" is modelled as −1 APL (the engine cannot refuse an activation — the VISION OF MADNESS substitution). PROCLIVITY FOR MURDER is two extra ActionDefs (D-021, so the AI cannot use them). WE HAVE COME FOR YOU resolves its D3 at the end of the activation rather than at the end of the Charge move (no post-action hook). CHAIN SNARE rolls when the snared operative is **activated**, not when it declares the Fall Back (`canPerformAction` is a pure query). |
| **warpcoven** | pinned | ⚠ shared validator — the printed `custom` SORCERER requirement and both `maxItem` weapon caps have **no branch in `validateRosterFor`**, so they are silently unenforced (`defaultRoster` is legal and does include a SORCERER) | 2/2 | 3/4 | 3/4 | 3/4 | 5/6 | 11/11 | Mindburn ✅ PSYCHIC ✅ Shield ✅ | 45 | ✅ 2 maps | The nine BOONS OF TZEENTCH are sliced out of the one printed faction rule at module load and chosen through the pure `setBoon` setter — Select Operatives still has no decision channel, so nothing applies until a boon is chosen. Eight boons are hooks; **Echoes from the Warp is reminder-only** (the reducer hard-refuses a counteracting operative's second action, and there is no order-change seam at that moment). Immaterial Flight is two `treatedAs` actions; Mutant Appendage is `Pick Up Marker (Mutant Appendage)` — its **"or mission actions" half is reminder-only**, because ops own those ActionDefs and a team cannot wrap them. Astartes is complete, counteract clause included. Mindburn tokens go through `onStatMod` (never the dead `StatMods.hit`) and are not cumulative with injured. **RAVAGE DESTINY is partial** — the forced "re-roll results of 6" works when shooting; the fighting/retaliating half is unreachable. **FATE ITSELF IS MY WEAPON is partial** — the 2D6 reserve, the "<9 discards the other" rule and the end-of-TP discard are real, but the *optionality* is not: a reserved dice is spent automatically and logged when it strictly helps (D-016). **MUTANT HERD is partial** — the pairing is recorded as an effect for the UI/AI, the same partial as Breach and Clear and Group Activation. SORCEROUS SCROLLS raises a real BOON-swap decision through `decisionHandlers` at activation; its "or counteracts" half is reminder-only (Counteract emits no hook). |

### Batch 3 — Imperium, non-Astartes

| team | data | selection | faction rules | strategy ploys | firefight ploys | equipment | unique actions | abilities | rare rules | tests | AI game | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **death-korps** | pinned | ⚠ shared validator — the printed `maxItem` constraint is a scraper mis-parse (`{kind:'maxItem', item:'times', max:4}`, from the `*` footnote about swapping a TROOPER for a 0CP ploy) and is unenforced (D-030); `defaultRoster` is otherwise legal but does not honour the WATCHMASTER's `loadoutMode: 'either'` | 1/1 | 3/4 (+1 partial) | 3/4 | 3/4 (+1 partial) | 2/3 (+1 partial) | 12/15 (+3 partial) | ConcealedPosition ✅ Detonate ✅ | 55 | ✅ 2 maps | Guardsmen Orders is ONE STRATEGIC GAMBIT with a four-way choice — one gambit id per order so the pick lands in `gambitsUsedTP` (the Kasrkin SKILL AT ARMS precedent), and the other three are withdrawn once one is issued. The four orders are sliced out of the single printed faction rule at load (the Legionary Marks of Chaos treatment). An order is an effect on the operative that replaces the previous one ("they only benefit from the most recent order"); IN LIFE, SHAME adds all four, Relay Orders re-broadcasts to the whole kill team and COMM-BEADS narrows the issue to one named operative. Every APL change (Relay Orders, Medic!, SIGNAL, GAS BOMBARDMENT) goes through `onStatMod` — which `aplOf` now consults — so it expires with its effect instead of leaking a permanent `aplMods` entry, and the module also pops `grantFreeAction`'s own un-popped `+1` at `onActivationEnd`/`onReadyStep` (REGROUP and the CHRONOMETER grant to the WHOLE kill team, which would otherwise leave every operative on APL 3 for the battle). Bring it Down! is live for shooting, fighting **and** retaliating (`onWeaponRules` is emitted by both sequences); it prints no duration, so the Punishing grant is modelled as end of turning point. The Mine marker is a real carried marker with its own Pick Up / Place actions and Detonate is fully implemented (the Phobos precedent) — the remote detonator is withdrawn from the universal Shoot action entirely, because Detonate prints "Don't select a valid target". Concealed Position is enforced per PROFILE through `onSelectWeapon`, and the long-las is additionally swapped for a copy without that profile once the SNIPER has shot, so neither the AI nor the action sheet can offer a profile the shot would then refuse (an `onSelectWeapon` refusal inside `startShoot` would be a rejected intent). Second in Command is a pure setter (D-017) rather than an auto-use, because using it permanently switches Directive off. Veteran Guardsman raises a real order decision through `decisionHandlers`. **Reminder-only:** IN DEATH, ATONEMENT entirely — no intent performs an action outside an activation and an incapacitated operative is removed inside the action that killed it (`onIncapacitated.freeActions` is emitted but never consumed, the D-024 gap); the "must end that move closer to that operative" / "closer to an opponent's drop zone or killzone edge" / "within this operative's control range" halves of REGROUP, the CHRONOMETER and Medic! (no seam constrains where a move ENDS); and SPOT's whole benefit, because the printed effect list is **missing from the JSON** (the text stops at "you can use this effect. If you do:"). **Partial:** Group Activation and Directive record the pairing as an effect the UI/AI reads — `EndActivation` hands the turn to the opponent after `onActivationEnd` fires and nothing runs later — the same partial as the Breachers' Breach and Clear, the Pathfinders' Group Activation and the Warpcoven's MUTANT HERD; and Bruiser's damage-ignore identifies "one normal success" by the striker's Normal Dmg, which cannot be told from a critical when a weapon's two Dmg stats are equal (the amount ignored is the same either way). |
| **exaction-squad** | pinned | ✅ shared validator — and `distinctOptions` (the GUNNER "each must have a different option" cap) **is** enforced by `validateRosterFor`, contrary to the batch brief; pinned by a test | 3/3 | 4/4 | 1/4 (+2 partial) | 1/4 (+2 partial) | 8/8 | 12/14 (+2 partial) | ConcealedPosition ✅ Repress ✅ | 65 | ✅ 2 maps (+ a mirror) | Marked for Justice is a per-player token on the mark (`giveToken`/`hasToken`), re-marked from `onIncapacitated`; its Punishing grant rides `onWeaponRules`, which `fight.ts` also reads through `sideWeapon`, so shooting, fighting AND retaliating are all live. Repress is both clauses — `onBlockAllocation` blocks two successes, and a retaliating Repress operative takes the first attack dice by flipping `seq.turn` as its pool is collected. Ruthless Efficiency uses the Pathfinders' `ignoreFriendlyControlRange` seam; because `onValidTarget` carries no weapon, its "excluding frag or krak grenades" carve-out reads the weapon recorded at `onSelectWeapon` (which `startShoot` emits immediately before the target check). GUILT REVEALS ITSELF widens the no-cover radius to 4" via `ignoreCoverTerrain`, then hands the cover save back at Roll Defence Dice by recomputing what the cover would have been — the printed "it doesn't remove their cover save (if any)" has no other expression. DISPENSE JUSTICE measures "moved more than its Move stat during the activation" exactly, by summing the inches `applyMove` writes to the log. Close Quarters Vigilance is its own `Shoot (Close Quarters Vigilance)` action (D-021) down the engine's point-blank path with the point-blank Hit penalty cancelled, and the visibility / Conceal-in-cover checks that path waives re-applied. Engendered Focus and Stubborn Subjugator are "you can ignore" rules, so under D-022 they ignore only *worsening* changes (injured included); Engendered Focus covers every field of `StatMods` plus the attack-dice count, but a change to its weapons' **Dmg** stats has no seam. **Reminder-only:** STROBING PHOSPHOR-LUMEN ("your opponent cannot re-roll their attack dice results of 1") — a `RerollGrant` cannot exclude a dice value, and its fighting/retaliating half is doubly unreachable because `fight.ts` emits no post-roll hook; EXECUTION ORDER — every clause hangs off interrupting an activation and there is no activation-order seam, so the ploy only records the marked enemy as an effect for the UI. **Partial:** EXACT PUNISHMENT and BRUTAL BACKUP enforce their "cannot select any other enemy operative" lock when shooting only, because `Fight` takes its target from the intent with nothing emitted before `startFight`; MANACLES rolls at the start of the enemy's activation rather than when it declares the Fall Back (`canPerformAction` is a pure query the AI runs many times per activation), which also misses its counteraction half; SPECIAL ISSUE SHELLS picks Saturate or Piercing 1 on a stated deterministic policy and never the third printed option, Torrent 1", because a weapon-rule read cannot raise a decision; Medic!'s "must end that move within this operative's control range" has no seam; Handler's simultaneous CYBER-MASTIFF activation is recorded as an effect, the same partial as Breach and Clear. APPREHEND is implemented at the 0AP the JSON records — the printed card almost certainly says 1AP (data problem below). |
| **novitiates** | pinned | ✅ shared validator (`uniqueExcept` + `maxCount`, both firing) | 1/1 | 3/4 (+1 partial) | 3/4 (+1 partial) | 4/4 | 4/5 (+1 partial) | 12/14 (+2 partial) | AntiPSYKER ✅ Riposte ✅ ZealousRage ✅ | 50 | ✅ 2 maps | Everything hangs off one Faith-point pool in `state.opState` (`faithPoints`/`gainFaith`/`spendFaith`) that eleven rules read or write. **Acts of Faith is complete**: the three ACTS are sliced out of the one printed faction rule at load (the Legionary Marks-of-Chaos exception) and offered as a real `PendingDecision` through `decisionHandlers` at all three printed moments — `onRollAttack` when shooting, `onDefenceDice` at the `defenceRerolls` step when an operative shoots a friendly NOVITIATE, and `onCollectAttackDice` for melee, where the window is opened as the pool is collected and ANSWERED after both sides have rolled because `fight.ts` emits no post-roll hook (`advanceFight` stops while anything is pending, so the handler always sees rolled dice). Points are deducted only when an ACT actually lands on a die, so a window that cannot apply — BLESSING while obscured, INTERVENTION with no fails — costs nothing. AUTO-CHASTISERS rides on the same decision as three extra "take N damage, use it free" options (never in the Roll Defence Dice window, which the printed rule does not name); ICON OF FAITH raises a second, different ACT from inside the handler and claims its once-per-turning-point allowance only when that second ACT lands; Militant Faith refunds the dearest ACT of the sequence. Which die an ACT lands on is deterministic (worst fail, then lowest normal), the same default `applyRetention` uses — the printed "one of your dice" is the player's choice and the decision vocabulary has no per-die channel. SANCTIFIED ROUNDS is auto-used on a stated policy under D-022 (spend a point only while more than one is held, so it can never eat the last point an ACT wants) rather than a decision on every shot. **Reminder-only clauses, four in total**: RIGHTEOUS ADVANCE's "must end that move closer to its closest enemy operative, its closest objective marker or your opponent's drop zone" and Medic!'s "must end that move within this operative's control range" (no hook constrains where a move ENDS — `onMoveRules` is unemitted and `onMoveDistance` only caps the allowance); BLINDING AURA's "If there are no other valid targets … that action ends (it's not cancelled, so that operative has still performed it)" (the reducer's only failure mode is cancel-and-revert plus a rejected intent); and both PSYCHIC halves of Null Rod — "For the effects of PSYCHIC actions, this operative cannot be selected and is never treated as being within those actions' required distances" (a team cannot reach into another team's unique-action target selection — the same clause the Celestian Insidiants reported) and "or use PSYCHIC additional rules" (abilities carry no per-use permission check and nothing tags one as PSYCHIC). **Partial**: AUTO-BROADCASTER's "cannot re-roll their attack dice" is exact when the enemy shoots (it clears every grant, Command Re-roll included) and unreachable when they fight or retaliate (D-031). **Riposte is implemented but through a log read**: `onBlockAllocation` carries neither the die nor a `dieWasCrit` flag, so "whenever you block with a critical success" is read off the `strikeOrBlock: Block with the critical success` entry the reducer logs one line before `resolveFightDie` — deterministic, but pinned to a label string. Timing shifts, all bounded rather than dropped: BLESSED REJUVENATION's "at the end of that action" and Divine Acquisition's mission-action trigger both fire at `onActivationEnd`, Medic!'s "cannot be incapacitated for the remainder of the action" holds for the activation, and GLORIOUS MARTYRDOM / BLAZING INFERNO are declared in advance and fire on their printed trigger (the Celestian GLORY TO THE MARTYRS shape — the reducer cannot be re-entered from inside `onIncapacitated`). Two extra `ActionDef`s carry the free Fight actions action restrictions forbid: `Fight (Whip Into Frenzy)` (0AP, gated on the EXACTOR's effect) and two `Fight (Absolution Through Destruction)` links, each its own id so "this takes precedence over action restrictions" works while "you cannot perform more than two Fight actions in succession" still refuses. **Anti-PSYKER is the Novitiates' own printed footnote (+1 to both Dmg stats AND Lethal 5+), scoped to NOVITIATE weapons — the Celestian Insidiants genuinely print a different rule; see D-025 below.** |
| **sanctifiers** | pinned | ⚠ shared validator — the printed `maxItem` cap ("You cannot select an option that includes a brazier of holy fire more than once per battle") has **no branch in `validateRosterFor`** (D-030) and is silently unenforced; `defaultRoster` happens to pick exactly one brazier, so the default kill team is legal | 2/2 | 3/4 (+1 partial) | 2/4 (+2 partial) | 3/4 (+1 partial) | 3/3 (+6 from abilities/equipment) | 14/19 (+4 partial) | Blaze ✅ TwinTorrent ✅ Wreathed ✅ | 52 | ✅ 3 killzones × 4 equipment | Everything hangs off two states: the **ORATOR** (an `endOfBattle` effect placed by the Ministorum Sermon, a faction rule offered as its own 0CP STRATEGIC GAMBIT through `gambitOptions`, which forces a live CONFESSOR) and `benefitting()` — within 3"/6" of a friendly ORATOR, *activated* within that distance, a MISSIONARY with a holy relic, or a PREACHER controlling an objective — which nine rules read. **Blaze is complete**: the token is handed out on crit damage (Devastating included), the D3 fires at `onActivationStart`, and both removal options are a real PendingDecision the **holder** answers through `decisionHandlers`. **Wreathed** is its own `Shoot (Wreathed)` action that skips the MIRACULIST itself and turns its Blast footprint into the target queue; the weapon is hidden from the universal Shoot by `availableWeapons` until that action arms it, so the AI can never misuse it. **Twin Torrent** queues the second target as a sequence secondary *and re-determines its cover/obscured*, so "roll each sequence separately" is exact; only the choice of second target is the deterministic default (D-016). **Fly** is three `treatedAs` ActionDefs measuring around Wall terrain in a close-quarters killzone; `src/ai/legal.ts` only offers `targetPos = op.pos`, which they refuse, so the AI never uses them (D-021's stated consequence). **Merciless Castigation** is `Fight (Merciless Castigation)`, locked to the enemy of the first fight and past the action restriction. Partial: **Lead the Procession** (grants land at the CONFESSOR's activation end — nothing runs at the end of an *action* — and the SERMON end-position clause has no seam; the 3" cap and the 1AP "free" Fall Back are real), **Commanding Declamation** (the D6 is pre-rolled at the enemy's `onActivationStart` because `canPerformAction` is a pure query the AI runs per candidate action; losing one action is modelled as −1 APL, the VISION OF MADNESS substitution, and the counteraction half is invisible because Counteract emits no hook), **Bladed Stance** ("that success must be used to block" is unenforceable), **Miracle** ("even if it's performed an action that prevents it…"), **RALLY THE FLOCK** (no end-position hook), **ARDENT ERADICATION** and **PURITY SEALS** (shoot only — `fight.ts` emits no `onRollAttack`, D-031; PURITY SEALS is also the D-018 `mode:'fails'` approximation), **UNWAVERING DEVOTION** (the Shoot half is exact through `onSelectTarget`, cover/obscured inheritance and the Blast/Torrent carve-out included; the Fight half has no seam). **Reminder-only: RELIQUANT › Imperial Cult Devotion** — a free action performed by an operative that is already incapacitated, which needs the queued free-action-on-death seam (`onIncapacitated.freeActions` is declared but never consumed — the Kommandos Boom! gap); no handler is registered. The CHERUB's "cannot use any weapons that aren't on its datacard" also has no seam (`weaponsOf` appends granted weapons *after* `availableWeapons`) and currently bites on nothing; its unique-action half is enforced. **MISSIONARY › Holy Relic is inferred from the recorded loadout** — the relic is not a datacard weapon and lives in `items[]`, so a MISSIONARY without a Brazier of holy fire is the one carrying it — which makes it live in the app but **inert in AI-driven games, where `playGame` never calls `applyLoadouts`**. |
| **ratlings** | pinned | ⚠ shared validator — the printed `maxItem` is silently unenforced (D-030) and its `item` is the literal word "times" (scraper bug below); `defaultRoster` is legal and fields FIXER + BATTLEMUTT + 3× BULLGRYN + BIG SHOT + BOMBER + HARDBIT + RAIDER + SNEAK + SNIPER | 0/1 (+1 partial) | 4/4 | 2/4 (+2 partial) | 4/4 | 3/4 (+1 data-blocked) | 12/18 (+4 partial) | — (none printed) | 54 | ✅ 2 maps | Scarper is D-015: a free Dash granted at each enemy `onActivationEnd`, lowest-id eligible operative, once each per TP, suppressed after the final activation — **partial**, its "cannot end that move within 3" of an enemy operative unless it's not visible to every enemy operative" has no end-of-move seam (the predicate is exported as `scarperEndPositionLegal` for the UI). The team's own `aplMods` upkeep pops each spent `grantFreeAction` +1, or Scarper would leave every Ratling on APL 3 for the battle. Three D-021 carve-outs: `Shoot (Sniper Positions)` (Silent is only read by the universal Shoot's raw-profile Conceal test, so the granted form needs its own action), `Charge (Hunter)` and `Pick Up Marker (Larcenous)`. FRONTLINE ASSAULT is complete including fighting and retaliating, because `onWeaponRules` is read by both sequences. SHIFTY recomputes cover with default options, which is what "taking precedence over Seek and Vantage" means. Brute lifts Light cover through `onValidTarget.ignoreCoverTerrain` and puts the cover save back at `onDefenceDice`, honouring Seek/Seek Light/Vantage — the printed second sentence. IMPROVISED ARMOUR promotes 5+ defence dice in the post-roll `onDefenceDice` emit because `addRolled` is called without `ClassifyOpts`; PURLOINED RATIONS reclassifies the already-rolled pool and carries the Hit change through re-rolls via `onStatMod`; both "until the end of that sequence" effects clear themselves because `endOfAction` is only swept at end of TP. Reminder-only: Munitorum Contacts (the reducer caps equipment at four before any hook), Grappling Hook (`onMoveRules` unemitted), LARCENOUS's "or mission actions" half (ops own those ActionDefs), Mine's "cannot trigger" and Tripwire's "end its action" (`checkMines` is core-only, so the trap springs at the activation boundary), and **SURVIVAL INSTINCTS' shooting half** (`allocateSavesOptimally` emits nothing — only the fighting half is reachable). Partial: Early Warning and Bayonet Charge resolve at the end of the activation (nothing runs at the end of an action), though Early Warning's 3" cap is enforced through `onMoveDistance`. SHARPSHOT's effect is exact but its trigger is widened from "shooting with a rifle" to "carries a rifle" (`onValidTarget` carries no weapon). **SPOT is data-blocked**: its printed effect list is truncated out of the JSON, so the token is placed and nothing is applied. |
| **inquisitorial-agent** | pinned | ⚠ shared validator — `uniqueExcept` and `maxCount GUN SERVITOR 2` hold and `defaultRoster` is legal, but the printed `custom` Piercing cap ("cannot include more than one weapon with Piercing 2 … not more than three with Piercing X combined") has no branch in `validateRosterFor` and is silently unenforced (D-030 family), and the 7 REQUISITIONED datacards in the JSON have no selection-list rows at all, so no REQUISITIONED operative can be fielded | 0/1 (+1 partial) | 3/4 (+1 partial) | 1/4 (+3 partial) | 4/4 | 3/5 (+2 partial) | 18/26 (+7 partial) | — (none printed) | 55 | ✅ 2 maps | The one faction rule is a selection + data rule: the six REQUISITIONED groups are sliced out of the printed text (`REQUISITION_GROUPS`) and the keyword replacement is already baked into every datacard (pinned), which is what makes every INQUISITORIAL AGENT rule reach a requisitioned operative; its one enforceable clause — "you cannot use ploys and equipment associated with a REQUISITIONED operative's former faction keyword" — is an `onSelectEquipment` filter, "you cannot use a REQUISITIONED operative's former faction rules" is reminder-only (a GameContext only ever registers this team's own module), and "you cannot select REQUISITIONED operatives from different groups" belongs to `validateRosterFor`, which has no rows for those datacards. INQUISITORIAL TOMES is four gambit combinations plus a real activation decision whose first option is "keep" (both TOME rules sliced from the printed text); Consecrated Tome outlives the INTERROGATOR. Machine is complete — the action ban, the `cannotRetaliate` effect, the assist subtraction, −1 APL for marker control and the Conceal-in-cover targeting veto with its within-2" exception. Hunter is a `Charge (Hunter)` action (D-021); PISTOL BARRAGE and SIGNAL are sibling action pairs because `state.sequence` is single-slot and action restrictions forbid a repeat; Adaptive Equipment lends the universal grenade allowance and restores it afterwards. **Expendable is reminder-only** (op scoring, owned by `src/core/ops/**`). Partial: **DENOUNCE**'s "cannot be activated" is modelled as "cannot perform actions" (no activation-refusal seam); **ABSOLUTE AUTHORITY** refunds the opponent's CP and leaves their ploy in `ploysUsedTP` so it cannot be re-used, but "their ploy isn't used" cannot be undone — their `UsePloy` has fully resolved before any response intent exists; **RELENTLESS IN PURSUIT** grants the free Reposition/Charge (D-015) and refunds when nothing qualifies, but "must end that move within 2"/within control range" has no seam; **THE EMPEROR'S WILL** zeroes every `StatMods` field, cancels the injured penalties and resets Atk to the printed value, but a Dmg-stat change lands at `onDamage`, where a shooting total is already aggregated; **CHASTEN** can only switch off a *unique action* (`canPerformAction`) because no seam disables another rule's hook bindings; **SCRY**'s own list of effects is absent from the JSON, so the action, its target selection, its Psychic Null exclusion and its duration are real but the effect carries no payload; **Group Activation** is recorded as an effect for the UI/AI (the Breach and Clear partial); **No Escape** rolls at the enemy's activation start rather than at the Fall Back declaration (`canPerformAction` is a pure query — the CHAIN SNARE precedent); **Hexorcise** strips shooting attack-dice and defence-dice re-rolls and a fight/retaliate re-roll survives, because `fight.ts` emits no `onRollAttack` (D-031); **Psychic Null**'s "cannot use PSYCHIC additional rules" has no seam and its "cannot be selected for PSYCHIC actions" half reaches only this team's own SCRY (the Celestian precedent); **Medic!** prevents the incapacitation, leaves 1 wound, shields for the rest of the action, hands out the free Dash and both −1 APLs, but "that activation ends" and "must end that move within this operative's control range" have no seam. Zealot and Weathered fire per `onDamage` call, so in a shoot they see the aggregated total rather than one attack dice. |
| **elucidian-starstrider** | pinned | ✅ shared validator — its printed `kind: 'every'` group is a FIXED roster (one of each named role, 3+1 VOIDSMAN), which the validator now enforces per row; before this batch it only checked the group total and `defaultRoster` fielded ELUCIA VHANE plus nine CANIDs as 'legal' | 1/2 (+1 partial) | 2/4 (+2 partial) | 3/4 (+1 partial) | 4/4 | 3/5 (+2 partial) | 13/16 (+3 partial) | — (none printed) | 47 | ✅ 2 maps | Leader is `inList` (deathwatch/wolf-scouts shape) and needs no team code. **Warrant of Trade is partial**: its seven sub-rules are sliced out of the one printed faction rule and run through the pure setter `useWarrantOfTrade` (D-017), which owns the shared "up to four times per battle, never the same one twice" budget — Coordinate gains the CP and **Explore is a real TP1 STRATEGIC GAMBIT** (D3 free Repositions with operatives wholly within the drop zone), while Consideration, Coerce, Bribe, Seize and Adaptable Terms are recorded and logged but reminder-only (the reducer caps equipment at four before any hook; Set Up Operatives has no ordering seam; there is no activation-skip seam; `initiativeRollModifiers.rerollOffered` is never consulted; the op swap belongs to `src/core/ops/**`). Explore's "wholly within 4\" of your drop zone" is reminder-only. **PRIVATEER SUPPORT ASSETS is complete**: the five weapons' stats come from the JSON and their weapon rules are sliced out of the printed `WR` rows (the scraper left `rules: []` — data bug below), `availableWeapons` keeps `grantedWeapons` equal to exactly the legal set on every read so the UI, `shotPlans` and `startShoot` can never disagree and the AI can never pick an illegal asset, the once-per-Firefight-phase and once-per-battle limits plus RAPID GUNNERY's re-use are enforced at `onSelectWeapon`, and the printed cover replacement ("a cover save if any part of its base is underneath Vantage terrain") lands at `onDefenceDice`. **STAKE CLAIM is partial** — the marker, the 3\" test and the dice promotion are exact when shooting; "fighting against or retaliating against" is unreachable (`fight.ts` emits no `onRollAttack`, D-031). **QUICK MARCH is partial** — its nominees come from the gambit's `data` (D-016) with a default that names only operatives which could not use an asset anyway, so the auto-taken choice is always free; "must end that move closer to your opponent's drop zone" is reminder-only. **WELL-DRILLED is partial** — the pairing is validated and recorded as an effect for the UI/AI, the same partial as Breach and Clear, Group Activation and MUTANT HERD. COMBINED ARMS tracks who has shot whom this turning point and grants a re-roll-any once. Equipment is complete: ARMOURED UNDERSUIT promotes a defence 4 directly in the pool (there is no defence-retention hook), HOT SHOT CAPACITOR PACKS is auto-used only while the shooter has more than 6 wounds left (the worst Hot result cannot then incapacitate it) with the +1 Dmg applied per unblocked dice at `onDamage` (D-019), IMPROVED COORDINATES UPLINK clears `seq.obscured` and adds Saturate, and RAPID GUNNERY is spent inside asset selection. **Loyal Companion is partial** — "force them to select this operative to fight against instead" is reminder-only (only `startShoot` has a target-substitution seam, the same gap as HOLY DEFENDER's Fight half), and the free Charge is granted at the end of the enemy's activation with its end-position clause unenforced. **Bladed Stance is partial** — it reorders the fight so the retaliating EXECUTIONER resolves first, but the engine builds the strike/block options so "that success must be used to block" cannot be enforced. **Medic! is partial** — it prevents the incapacitation, sets 3 wounds, shields for the action and grants the free Dash, but "must end that move within this operative's control range" has no seam and "that activation ends" is modelled as spending the rest of its AP. **GATHER is partial** — one free action out of Dash / Reposition / Pick Up Marker / Place Marker, because the engine cannot interleave a marker action inside a move and a second `grantFreeAction` would add nothing (APL clamps to ±1). **CALIBRATE VOLTAGHEIST is partial** — Charge is exact (Lethal 4+ on the voltaic pistol, cleared at the start of its next activation, on incapacitation and on re-use), Field fires at the end of the enemy's activation rather than at the end of each move action (no end-of-action hook). Merciless and Disciplinarian are bound at priority 40 so their "if the weapon already has that weapon rule → Ceaseless instead" upgrade sees grants from ploys and equipment; Merciless covers shooting, fighting and retaliating because `onWeaponRules` is emitted by both sequences. Crewmen widens `onCounteract` for a Conceal-order VOIDSMAN (once per turning point, only while no asset has been used that turning point), restricts it to Shoot and strips its card's ranged weapons so it must use an asset. UNCOMPROMISING FIRE is the printed 1AP action plus its own 0AP `Shoot (Uncompromising Fire)` (D-021), relic laspistol then artificer shotgun (close range), with "or vice versa" enforced through `canPerformAction`. |
| **spectre-squad** | pinned | ✅ shared validator | 1/2 (+1 partial) | 2/4 (+2 partial) | 3/4 (+1 partial) | 4/4 | 5/6 (+1 partial) | 7/13 (+4 partial) | ConcealedPosition ✅ | 62 | pending soak | Everything hangs off Elite Fieldcraft's interrupt, which the engine cannot take at its printed moment: `offerGuardInterrupt` (`src/core/reducer.ts`) is the only thing that runs after an action and it emits nothing, so the interrupt is taken at `onActivationEnd` and the free action is D-015's extra AP on the interrupting operative's own next activation. The rest of the rule is exact — the 1/2-point Ready-step gain (VOX-OPERATOR in the killzone and unengaged), the end-of-TP discard, once per enemy activation, once per friendly operative per TP, the TROOPER's Cool-Headed 0-point interrupt, SHARP REACTIONS widening it to a Conceal-order enemy within 8", and a `Shoot (Elite Fieldcraft)` ActionDef (D-021) that changes the order, refuses every enemy but the interrupted one through `onValidTarget`, and takes the point-blank path when engaged with that enemy alone. **Reminder-only**: Elite Fieldcraft's and DISAPPEAR's identical "cannot end this move closer to that enemy operative or your opponent's drop zone" (plus its close-quarters Wall/teleport rider) — no hook constrains where a move ends. **ISSUE MISSION is partial**: the flag and its "it cannot counteract during the turning point" consequence fire, but an expended operative still cannot perform the free action (no out-of-activation action intent). **PATIENCE is partial** — the Relentless half for the last friendly operative activated is live; "you can skip that activation" needs an intent a STRATEGIC GAMBIT cannot reach (Phobos' PATIENT AMBUSH works only because it is a firefight ploy used at that moment). **PREPARED DEFENCE is partial** — the "block two unresolved successes" half is exact through `onBlockAllocation`; the "resolve one success before the normal order" half reorders the fight (the Bladed Stance precedent) but "that success must be used to block" is unenforceable because the engine builds the strike/block options. Camo Cloaks takes the additional-cover-save branch deterministically (scout-squad precedent) and stands down under Vantage. Melta Mine is a real carried marker with its own Pick Up/Place actions, the enemy-control-range placement ban and the free Dash (Phobos precedent); Proximity Mine's 2D6+3 lands but springs at the activation boundary, so "end its action" is lost. Reminder-only abilities: **Pre-Deploy** (the drop-zone test runs before `onDeploy` and `extraZones` is never read) and **Prepared Killzone** (`SelectEquipment` refuses a fifth option before any hook). **Expendable is partial** — the action ban, counteract, retaliate (the core `cannotRetaliate` effect), assist, marker contesting and "ignore this operative's control range" when selecting a target are all live; "operatives can move through this operative", "your opponent can ignore this operative's control range at any time" and the kill/elimination-op exclusions (`src/core/ops/**`) are not. Medic!'s "must end that move within this operative's control range" and Scout Terrain's 2" climb-ignore (needs `onMoveRules`) are the last reminder-only clauses. The VOX-RELAY BEACON's printed **Move 0"** is pinned by a test; `moveOf` clamps it to 4" but Expendable forbids every move action so nothing reads it. |All twenty-four batch-1, batch-2 and batch-3 teams are implemented. `tests/teams/selection.test.ts`
covers every team's printed selection requirements, and `tests/teams/soak.test.ts` rings each batch
separately (8 pairings × 2 maps per batch, 48 games) with zero rejected intents.

## Engine seams added for team rules

All additive, all reported:

| seam | why |
| --- | --- |
| `onWeaponRules` hook + emit in `effectiveRules` (`src/core/sequences/shoot.ts`) | "friendly X operatives' weapons have the Severe/Ceaseless/Accurate 1 … weapon rule" is the single most common team-rule shape. Emitted on every read, so a grant can never go stale mid-sequence. |
| `onPloyUsed` hook + emit in `UseGambit`/`UsePloy` (`src/core/reducer.ts`) | A ploy's *immediate* effect (place a marker, grant an effect, hand out a free action) had no seam at all — ploys could only be read back from `ploysUsedTP`. |
| `onBlockAllocation` payload gains `blocks` and `normalsCanBlockCrits`, emitted in `resolveFightDie` | The rare `Shield` weapon rule ("each block can block two successes") and the AoD `DUELLER` tactic ("normal successes can block critical successes") both change block allocation. |
| `TeamModule.register(reg, player, ctx?)` | Hook handlers get no `GameContext`, but team rules need datacards (keywords, bases) and base-to-base geometry. |
| `onSetUpAgain` added to `HOOK_NAMES` | Referenced by `src/core/ops/crit/energyCells.ts` (another agent's file) but not yet declared — added so the tree typechecks. |
| `onSelectTarget` hook + emit in `startShoot` (`src/core/sequences/shoot.ts`) | "…becomes the valid target instead (even if it wouldn't normally be valid for this)" — Celestian Insidiants' HOLY DEFENDER and Pathfinders' SAVIOUR PROTOCOLS. The substitute inherits the original target's cover/obscured, exactly as both rules print it. Also carries the Kommandos' Explosive shot, whose primary target is the BOMB SQUIG itself. |
| `onValidTarget` payload gains `viewFrom` and honours the (previously dead) `forceVisible`; the emit moved above the visibility and friendly-control-range checks | The Hierotek Circle's Magnify rule determines "a valid target, cover and obscured" from ANOTHER operative. `forceVisible` was declared but the early return fired before the hook was emitted, so it could never take effect. |
| `onValidTarget` payload gains `ignoreFriendlyControlRange` | "Having other friendly PATHFINDER operatives within an enemy operative's control range doesn't prevent that enemy operative from being selected" (SUPPORTING FIRE). |
| `onSelectWeapon` is now emitted (in `startShoot`) | The hook was declared but never emitted, so the rare `Concealed Position` rule — a PROFILE-level restriction that `availableWeapons` (per weapon) cannot express — was a silent no-op. |
| `onStunTest` hook + emit in the universal Stun Grenade action (`src/core/equipment/grenades.ts`) | Celestian Insidiants' PSYK-OUT GRENADES adds damage to a stun test on a 3+ (the whole dice result against a PSYKER). The stun test is rolled inside the universal action and had no seam at all. |
| **Batch 2** — `onCounteract` is emitted **before** the Engage-order test, which becomes the event's default (`allowed: o.order === 'engage'`) instead of a pre-filter; `whoActivates(state, ctx?)` consults `counteractCandidates` when given a context (`src/core/phases.ts`) | "This operative can counteract regardless of its order" — printed on the Astartes faction rule of **twelve** kill teams (ten implemented). The hook could previously only *narrow* the list the core had already computed, so every one of those clauses was unreachable, and batch 1 shipped Angels of Death and Plague Marines with the handler registered but inert — the silent no-op architecture rule 5 forbids. `whoActivates` had to move with it or the candidate list and the turn test would disagree; as a side effect the two now also agree about the On Guard lockout, which `whoActivates` did not previously check. |
| **Batch 3** — `onSelectWeapon` is emitted from the Shoot action's `check` as well as from `startShoot`, sharing one `emitSelectWeapon`; the payload gains `dryRun` | "This operative can only use this weapon the first time it's performing the Shoot action during the battle" (rare `Concealed Position`) sits on a sniper rifle's **`concealed` profile alone**. `availableWeapons` is per WEAPON, so the only hook that can express it was `onSelectWeapon` — which fired inside `perform`, so the AI offered a shot the sequence then refused and it landed in `state.rejected`. Caught by the batch-3 soak ring, and it also exposed Kasrkin removing the Sharpshooter's whole rifle instead of the one restricted profile. `dryRun` exists because 6 of the 10 `onSelectWeapon` handlers do bookkeeping rather than gating, and a `check` must never mutate. |
| **Batch 3** — rare weapon rule text is stored per team (`byTeam` in `src/core/weaponRules.ts`), and `rareRuleTextFor` resolves a rule against its own team's printed source | The generated registry keeps ONE definition per rule id. Anti-PSYKER adds a Dmg bump on the Novitiates' Condemnor and does not on the Celestian Insidiants' own footnote (**this answers D-025**); Detonate names a different marker for each of its four teams. Every team was being shown one team's wording. |

## Data problems found while implementing batch 2

Implementing a team is the only thing that reads every byte of its JSON, so each batch finds
scraper bugs. These are **in the committed data**, not in the modules:

- **The section overrun hits all 48 of 48 teams.** Every team's LAST strategy ploy has the entire
  "Firefight Ploys" section appended to its `text`, and every team's LAST firefight ploy has the
  entire "Faction Equipment" section appended — thousands of characters where a hundred belong.
  `trimTrailingSection` in `src/teams/data.ts` cuts both at the heading, so the app and the
  modules quote the right text, but **the committed JSON is wrong for every team** and any
  consumer reading the file directly gets a page dump. The load-time patch is a workaround; the
  fix belongs in `tools/teams/normalise.py`, at the missing terminator for the final item of a
  section. The same overrun also drags a stray `weapons: []` array from the following equipment
  entry onto those two ploys in several teams.
- **`deathwatch`: "up to one GRAVIS operative" was unenforceable and the default roster broke
  it.** GRAVIS is a datacard keyword shared by three list rows, not a selection role, so the
  validator's role match never fired and `defaultRoster` produced a two-GRAVIS kill team it then
  accepted. **Fixed** in `src/teams/selection.ts`: a `maxCount` whose role matches no list row
  falls back to a keyword match. Pinned by a test.
- **`nemesis-claw`: an ability is mis-titled and its rare rule is truncated to match.**
  `nemesis-claw.night-lord-fearmonger.poison-objective` is named "POISON OBJECTIVE" but its text
  is the second sentence of **Terrorchem** ("Whenever an operative that has one of your Terrorchem
  tokens is activated, inflict D3 damage on it."). The real POISON OBJECTIVE action is separately
  and correctly present. `_rare-weapon-rules.json`'s `Terrorchem` definition stops one sentence
  early to match — compare `Poison`, which carries both sentences.
- **`wolf-scouts`: HUNTER'S SENSES is truncated.** The text ends at "Select one of the following
  rules for this operative's instigator bolt carbine to have…:" with the bullet list absent, so
  the selectable rules cannot be pinned. `notes[]` is `[]` and `validate.py` did not flag it —
  `docs/TEAM-DATA.md` § "What the source did not give us" should gain a `wolf-scouts` row.
- **`legionary`: GRISLY MARK places a marker with no printed effect.** The action text is only
  "Place your Grisly marker within this operative's control range" plus its restrictions, and
  nothing says what the marker does, though `markerGuide` lists the token. Likely a dropped
  SHRIVETALON datacard clause — worth checking the October '25 PDF.
- **`_rare-weapon-rules.json`: `Detonate` is stored with one team's wording.** The definition says
  "your **Mine** marker" throughout (the Imperial Navy Breachers' text), but the Phobos Strike
  Team datacard prints "your **Explosives** marker". Detonate is per-team text, so the shared
  registry entry shows a Phobos player the wrong marker name.
- **`uniqueActions[].keywords` is never populated by the scraper.** Several actions begin
  "PSYCHIC." in their text, but the field the schema provides for it is empty everywhere, so no
  rule can filter on PSYCHIC actions — anti-psyker rules and the Celestian Insidiants' PSYCHIC
  carve-out cannot see them.
- **`warpcoven`: the `^3` half-selection cap looks wrong.** `{kind:'halfSelection', group:'^3',
  max:1}` caps a Warpcoven kill team at two TZAANGOR operatives in total, but four entries carry
  the footnote and its wording is the two-entry form used by the Kommandos GROT + BOMB SQUIG pair.
  **Worth a human check against the card** — if each TZAANGOR simply costs half a selection, `max`
  should be the group's own count.
- **`murderwing`: equipment-granted unique actions are unstructured.** SLICE FROM ABOVE, CLAWED
  CHARGE and VOX-CRY are run-together strings inside the equipment `text` ("SLICE FROM ABOVE1AP\n…")
  with no id/name/ap, unlike datacard `uniqueActions`. A `uniqueActions: []` array on equipment
  entries would let a module use the `uniqueAction()` helper instead of re-deriving AP from prose.
- **Several teams have a null `selection.leader.role`/`datacardId`** while `leaderList` holds the
  real entries (phobos-strike-team, legionary, warpcoven). Cosmetic — the validator copes — but a
  roster error reads "exactly 1 LEADER operative" instead of naming the role.
- **Checked and NOT bugs**, recorded so a future re-scrape diff is not "fixed": Melta bomb 5/3,
  Skysear meltagun 6/3, Doombolt 4/2 and the Terrorchem vial's `dmgC: 0` all have Critical Dmg at
  or below Normal Dmg. That is how KT24 prints weapons whose crit pays out through Devastating.

## Bundle size

`data/teams/**` is code-split by `src/ui/data.ts` (dynamic import per team), but **a team added to
`src/teams/data.ts` is also statically imported**, and Vite warns that a statically-imported module
cannot move into another chunk. So every implemented team's JSON lands in the main bundle: 552 kB /
135 kB gzipped after batch 1, 852 kB / 193 kB after batch 2, and it grows again with batch 3's
eight. The remaining 24 teams continue the trend.
`teamData()` is synchronous and every module calls it at import time, so fixing this means making
team data async or generating a small per-team static slice — a real design change, flagged here
rather than papered over.

## Data problems found while implementing batch 3

Full detail per team is in the notes column above. The ones that matter beyond their own team:

- **Four unique actions print no AP cost, and only one is flagged.** `notes[]` records the
  Exaction Squad's `APPREHEND`, but `DEPLOY NUNCIO-AQUILA` and `R-VR COMMAND` on the same team are
  also recorded at `ap: 0` with no note, as are the Pathfinders' `MEDIKIT` and `PULSE
  ACCELERATOR`. `validate.py` should flag every 0AP unique action, not some of them.
- **Four printed effect lists are truncated to their lead-in.** Death Korps `SPOT`, Ratlings
  `SPOT`, Inquisitorial Agent `SCRY` and Wolf Scouts `HUNTER'S SENSES` all end at "…select one of
  the following effects:" with the bullet list absent. The scraper drops a `<ul>` that follows a
  colon inside an action body. **Four rules cannot be implemented at all because of it** — this is
  the single highest-value scraper fix outstanding.
- **The Inquisitorial Agent's four firefight ploys have "1CP" glued onto their name AND their id**
  (`ABSOLUTE AUTHORITY1CP`, `inquisitorial-agent.fp.absolute-authority1cp`). Ids are load-bearing,
  so this one cannot be fixed by trimming at read time the way the section overrun is.
- **`uniqueActions[].keywords` is empty everywhere**, though many actions begin "SUPPORT." or
  "PSYCHIC." in their text. Confirmed again on ratlings, spectre-squad and nemesis-claw. Any rule
  that filters on SUPPORT or PSYCHIC actions has to sniff the text.
- **`maxItem` items are sometimes the literal word "times"** (`death-korps`, `ratlings`), from
  `normalise.py` mis-parsing a footnote sentence beginning "Up to three times, instead of
  selecting one of these operatives…". Harmless today only because `maxItem` is unenforced (D-030).
- **The Spectre Squad's section overrun has a different shape**: its last strategy and firefight
  ploys carry a fully POPULATED `weapons` array copied from the equipment section, where other
  teams get a stray empty one. `trimTrailingSection` repairs the text but not the `weapons` field.
- **The Spectre Squad's SNIPER OVERWATCH equipment weapon lost its rules** — the printed
  "Devastating 2, Heavy (Dash only), Saturate, Silent" WR row parses to `rules: []`, so it reads
  as a vanilla shot.
- **The Elucidian Starstrider's PRIVATEER SUPPORT ASSET weapon table likewise parses with
  `rules: []`** for all five weapons, losing every printed WR.
- **`TeamData` does not expose `notes[]`**, although every team JSON carries it — so a module or
  test that wants to pin the scraper's own warnings has to import the raw JSON separately.
- **The Novitiates' `defaultRoster` fields four MILITANTs and only 6 of 12 datacards.** Legal, but
  it means the shared soak never exercises the PENITENT, PRECEPTOR, PRONATUS, PURGATUS or
  RELIQUARIUS. Worth a roster that maximises datacard coverage for soak purposes.
- **`markerGuide` is empty for the Elucidian Starstrider** though STAKE CLAIM places a Claim
  marker, so the UI has no name or art for it — the same class of gap as the Legionary Grisly
  marker.

## Known engine gaps (not team bugs)

- **Team-raised choices go through `GameContext.decisionHandlers`** (a team pushes its handler in
  `register`): Celestian Insidiants' BENEDICTION / Zealous Ultimatum / AUTO-FLAGELLATOR and the
  Hierotek Circle's Reanimation Protocols all raise their own `PendingDecision` kinds. What is
  still missing is a decision channel during **Select Operatives** (Angels of Death's CHAPTER
  TACTICS still need the pure setter) and any way to ask a question that is not a finite option
  list — a position on the killzone floor is still taken from `UsePloy`/`UseGambit` `data` with a
  deterministic, logged default (D-016).
- **`RosterPick.loadoutId` / `weapons` are dropped by `SelectRoster`.** The shared validator computes
  each operative's weapons; `applyLoadouts` records them under `state.opState['loadout']` for the UI.
- **No "perform an action outside your activation" intent.** Free-action grants are modelled as one
  extra AP restricted to the named action (`grantFreeAction`, docs/DECISIONS.md D-015).
- **CP is deducted before `onPloyUsed` fires**, so "this ploy costs you 0CP" abilities are refunds.
- **`StatMods.hit` from `onCollectAttackDice` is never read.** `shoot.ts` computes `hit` from
  `hitOf()` and only uses `mods.atk`; `fight.ts` does the same. A `+1 to hit` written there is a
  silent no-op — it must go through `onStatMod` (which `hitOf` does consult), reading
  `state.sequence` for the weapon/target context. **This makes the Imperial Navy Breachers'
  GRENADIER ability (`src/teams/imperial-navy-breacher/index.ts:190-195`) inert**: "improve the
  Hit stat of that weapon by 1" never happens.
- **`aplOf` ignores hooks entirely** (`src/core/state.ts:78-83`): it reads `op.aplMods` and
  nothing else, so `StatMods.apl` is dead and "ignore any changes to its APL stat" rules have to
  clear `op.aplMods` at the moments they can reach (Kommandos SHAKE IT OFF, Breachers REBREATHERS).
- **Declared but never emitted**: `onBattleSetup`, `onAttackDiceRetained`, `onFreeActions`,
  `onOrderChange`, `onMoveRules`, `onSetUpAgain`, and `onIncapacitated.freeActions`. Each is a
  silent no-op for any rule that registers against it (architecture rule 5 says a hook never is).
  `onOrderChange` in particular means "this operative cannot have an Engage order" (Kommandos
  GROT, BOMB SQUIG) has to be corrected at `onActivationStart` instead of refused at the choice.
- **`fight.ts` emits no post-roll hook at all.** `offerRerolls` builds its grants from
  Balanced/Ceaseless/Relentless plus Command Re-roll and never emits `onRollAttack`, which
  `shoot.ts` does emit. **Six of the eight batch-2 teams reported this as their top missing
  seam** — Scout Squad's Designate Target, Wolf Scouts' Cast the Runes, Legionary's CHAOS
  TALISMANS, Nemesis Claw's Screecher, Warpcoven's RAVAGE DESTINY and FATE ITSELF IS MY WEAPON,
  and Phobos' PURITY SEALS each print "shooting, fighting or retaliating" and each works only
  when shooting. **It was deliberately NOT added in batch 2**: 15 of the 17 existing
  `onRollAttack` handlers across both batches do not guard on `ev.ctx.type`, so emitting it in
  fights would silently change melee behaviour for all of them — some would become more correct,
  others would start firing where their printed rule says shooting only. It needs its own change
  with all 17 handlers audited, and it is the highest-value seam left.
- **A mandatory re-roll cannot be expressed.** Every `RerollGrant` is offered as an optional
  decision with a `keep` option, so "your opponent **must** re-roll their attack dice results of
  6" (Warpcoven's RAVAGE DESTINY) has to re-roll the pool itself inside the hook. A
  `forced?: boolean` + `value?: number` on `RerollGrant` would fix it.
- **There is no marker-trigger hook.** `checkMines` is inside `applyMove` (`src/core/actions.ts`),
  hard-wired to `kind: 'mine'`, D3+3 damage and any player, and emits nothing. Every team mine —
  Kasrkin's Melta/Proximity Mine, Scout Squad's Booby Trap, Phobos' and Wolf Scouts' Proximity
  Mine — therefore springs at an activation boundary rather than mid-move, so the printed "end its
  action" can never be honoured. Phobos **did** close the other half of the Kasrkin gap: a mine is
  now a real carried marker with its own Pick Up / Place actions.
- **`onMoveRules` being unemitted is now the single most-cited gap** (4 of 8 batch-2 teams):
  Murderwing's Jump Pack, both Phobos REIVERs' and the Deathwatch Headtaker's Grav-chute and
  Grapnel Launcher, and Scout Squad's Grapnel Launcher all need per-leg climb/drop control.
  `validateMove` keeps its `legs` (which know climb/drop/jump) to itself.
- **Nothing runs at the end of an action**, so "at the end of that action" rules are bounded to
  the activation instead (Legionary's MALIGNANT AURA, Nemesis Claw's WE HAVE COME FOR YOU,
  Murderwing's WARP FUEL).
- **`validateRosterFor` has no branch for `kind: 'custom'` or `kind: 'maxItem'`** — both are still
  silently unenforced (D-030). `maxItem` appears in warpcoven, canoptek-circle, hunter-clade,
  void-dancer-troupe, hand-of-the-archon, sanctifiers, xv26, death-korps and ratlings; `custom` in
  warpcoven and inquisitorial-agent. *(Batch 3 note: `distinctOptions`, `sameAsAbove`, `every` and
  keyword-scoped `maxCount` ARE now enforced — see D-029, D-034, D-035.)*
- **A unique action's target legality must live in `check`, not `perform`.** `src/ai/legal.ts`
  `missionCandidates` offers friendly AND enemy targets and only re-runs `def.check`, so anything
  `perform` refuses becomes a rejected intent. Found twice while soaking batch 1's second half —
  once in this batch's own SPEAK OF HER DEEDS, and once in Plague Marines' POISONOUS MIASMA
  (`check` accepted any `targetOperativeId`, `perform` then refused a friendly one); both now
  validate in `check`. Worth a lint: no `perform` should return a reason its `check` cannot.
